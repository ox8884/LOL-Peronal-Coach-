# LoL Personal Coach CLI

Riot Match API로 최근 전적을 분석하고, [blitz.gg](https://blitz.gg) 현재 패치 메타 빌드(룬/스킬/아이템/승률)를 가져와 **맞춤 코칭**을 출력하는 Python CLI/GUI입니다.


[![Release](https://img.shields.io/badge/릴리스-latest-3B8ED0?logo=github)](https://github.com/ox8884/LOL-Peronal-Coach-/releases/latest)
[![Download](https://img.shields.io/badge/⬇%20인스톨러%20다운로드-Releases-81C784)](https://github.com/ox8884/LOL-Peronal-Coach-/releases/latest)

> **설치 파일은 [Releases](https://github.com/ox8884/LOL-Peronal-Coach-/releases/latest) 페이지에서 받을 수 있습니다.**
> `.env`(API 키)는 설치 파일에 포함되지 않으며, 첫 실행 시 입력하면 자동 생성됩니다.
> 📄 **기능 소개 페이지**: [**롤 실전 코치 — 기능 가이드**](https://ox8884.github.io/LOL-Peronal-Coach-/features.html) — 브라우저에서 바로 열어 보는 전체 기능 가이드 (GitHub Pages 렌더링)

## Features

1. **Riot ID → PUUID + 최근 N게임 분석** (KDA, CS, 승률, 포지션, 챔프별 성적, **모드별 구분**)
2. **현재 게임 중인지 확인** (Spectator V5)
3. **챔피언 메타 빌드** — 협곡·칼바람·아수라장 전부 **blitz.gg** 단일 소스
4. **내 최근 해당 챔프 플레이 vs 메타 비교** + 자연어 조언 (`--mode aram` 지원)
5. **선택형 AI 코칭** — 현재 패치 기준 조합 분석, ARAM 실시간 조합 코칭, 모델 선택


### v1.6.59

- **👁 아수라장 제시 증강 화면 읽기** — 맵에서 3장이 뜨면 붙여넣지 않고 판정
  - 레벨 3·7·11·15가 되면 화면 중앙을 Windows 한글 OCR로 읽어 카탈로그와 맞춤
  - 게임 포커스 중에도 `Ctrl+Shift+A` 로 다시 읽기
  - 밴픽 LCU 404를 “클라이언트 없음”으로 오인하던 안내 수정 — 증강은 밴픽이 아니라 맵에서 뜸


### v1.6.58

- **⚡ 밴픽 중 아수라장 자동 브리핑** — 앱만 켜 두면 LCU 밴픽을 상시 추적
  - 내 챔프가 잡히는 순간 ARAM 탭으로 전환하고 TOP 3·6슬롯을 바로 표시 (밴픽 버튼 불필요)
  - 제시 증강이 뜨거나 리롤되면 판정만 갱신 · 협곡 밴픽은 건드리지 않음
  - API 키·전적 로드 없이 동작 (롤 클라이언트만 있으면 됨)


### v1.6.57

- **🎯 아수라장 시작 시 자동 브리핑** — 큐 2400이 감지되면 ARAM 탭으로 전환하고 내 챔프 + 희귀도 TOP 3 + 6슬롯을 바로 표시 (자동 검색 버튼 불필요)
  - 시작 토스트가 칼바람과 아수라장을 구분 (`아수라장 · 내 챔피언 X`)
  - 승패 예측 폼은 아수라장 전적 우선 (표본 부족 시 칼바람 → 전체)
- **🧱 LiveSession 분리** — 종료 워처·리메이크 게이트·모드 판정을 tk 없는 `gui/live_session.py`로 이동 (1단계 구조 정리)


### v1.6.56

- **🛠 라이브 정산·정찰 버그 수정** — 리메이크/조기항복이 본판 예측·팀운·디스코드를 잡아먹던 경로를 닫음
  - 같은 로스터 예측은 가장 최근 것만 소비하고 오래된 잔여는 삭제
  - 5분 미만·조기항복 경기는 성적표/팀운/복기 카드를 보내지 않음
  - 새 게임 ID가 보이면 종료 워처를 교체해 본판 종료 감지가 빠지지 않음
  - 이기고 활약이 적으면 "네가 캐리했습니다"로 뒤집히지 않음
  - 정찰 이름을 Spectator-V5 `riotId`로 표시, 상세 조회도 페이싱·429 시 중단
  - 디스코드 오류 토스트에 웹훅 토큰이 나오지 않음
  - 로컬 전적 본인 판정은 puuid 우선, 이름은 `#태그`를 빼고 비교


### v1.6.55

- **🔍 10인 자동 정찰 + 리드 칩** — 게임 시작 시 양팀 9명 최근 전적을 자동 스캔해 브리핑 (loldosa식 정찰)
  - 리드 칩: "오늘 N판째(W승 L패)" · "방금 패배 후 재큐 — 빡큐" · "원챔 X (5판 중 4판)" · "폼 핫/콜드" — 전부 결정적 계산
  - 표본 3판 미만이면 침묵 (말하지 않는 원칙) · 시작 시 토스트 + 디스코드 정찰 카드
  - Riot rate limit 안전: 플레이어당 리스트 1회 + per-puuid 30분 TTL 캐시 + 순차 pacing + 실패 스킵


### v1.6.54

- **🔒 보안·구조 정리** — 전체 코드 리뷰 반영
  - LLM 요청 프록시 격리 강화: `secure_session(trust_env=False)` 전환 — 환경 프록시/CA로 키가 흐르는 경로 차단 (요청별 프록시 무력화와 이중 방어)
  - 설정창 디스코드 웹훅 URL 마스킹(show="•") — API 키와 동일 정책
  - 디스코드 전송 5개 래퍼 → 공통 `_post_discord_card` 헬퍼 통합 (제목·PNG 전부 지연 호출 — 웹훅 미설정 시 부수 효과 없음)
  - me_tab.py 1,883줄 → 매치 상세 뷰를 `me_detail_mixin.py`(671줄)로 분리
  - ruff format 전면 적용 (76개 파일 포맷 완료)


### v1.6.53

- **⚖️ 이 판 누구 탓 % — 팀운 정산** — 게임이 끝나면 책임을 나·팀·상대 3분해로 정량화 (예: 나 13% · 팀 68% · 상대 19%)
  - 점수 = KDA(어시 0.7 가중) + 팀 내 데미지 비중 — 이해 가능한 투명 공식, 표본(팀원 3명 미만)이 없으면 말하지 않음
  - 판정 한마디: "네 탓 아님" / "내 탓이 큽니다" / "팀 전체가 같이 무너졌습니다" / "네가 캐리했습니다"
  - 종료 시 토스트 + 디스코드 팀운 정산 카드 (나/팀/상대 3색 바)


### v1.6.52

- **🔮 승패 예측 → 🧾 성적표 정산 루프** — 게임 시작(로딩) 시 양팀 조합 + 내 최근 폼으로 승률 예측, 끝나면 예측 vs 결과 성적표 카드
  - 예측: 앞라인 차이·힐/유틸·풀 AD/AP 패널티 등 조합 균형 ±16% + 내 폼(최근 5판+ 표본) ±12%, 20~80% 클램프 — 전부 코드 계산 (LLM 없음, 표본 부족 항목은 조용히 제외)
  - 시작 시 토스트 "🔮 승률 예측 N%" + 디스코드 예측 카드, 종료 시 "🧾 예측 성적표 — 적중/빗나감" + 이 판 한마디
  - 예측은 로스터 시그니처로 소비 — 닷지·리메이크는 잘못 매칭되지 않고 6시간 지난 미소비 예측은 자동 정리


### v1.6.51

- **⚡ 증강 실시간 판정** — 아수라장 밴픽에서 증강 3개가 뜨는 순간 LCU가 감지해 **즉시 판정** (토스트 "🎯 증강 판정 — X 선택" + 디스코드 판정 카드 자동 전송)
  - 리롤하면 새 목록으로 자동 갱신 (이전 자동 입력만 교체, 수동 수정은 보존)
  - 판정은 카탈로그/Blitz 티어 기준 — 제시된 증강 안에서만 1~3순위 + 회피 권장 카드 렌더
  - 화면 캡처·OCR 없음 — 롤 클라이언트 로컬 API(LCU)의 밴픽 세션에서 직접 읽음


### v1.6.50

- **📮 디스코드 복기 카드 자동 전송** — 게임이 끝나면 킬 지도·붕괴 스냅샷·복기 요약을 담은 카드가 디스코드 채널로 자동 도착 (loldosa식 UX)
  - 설정창에서 웹훅 URL 붙여넣기·테스트 전송·자동 전송 on/off 전부 GUI로 (명령줄 불필요)
  - CLI `lol-coach discord set/url/test/clear` 도 제공 (웹훅 토큰 마스킹·https+디스코드 호스트 검증·8MB 한도·429/5xx 재시도)
  - 카드에는 게임 모드(칼바람·아수라장·협곡)·KDA·킬 지도·붕괴 스냅샷·이 판 핵심/잘한 점/다음 판 행동/교훈 포함 — 아수라장(queueId 2400)도 동일 지원


### v1.6.49

- 🔌 **키 없는 로컬 전적 모드** — Riot API 키가 없거나 만료돼도 롤 클라이언트(LCU) 로컬 API로 내 전적·복기·트렌드가 그대로 동작 (타임라인 엔드포인트가 있으면 킬 지도까지)
- 🔑 **개인 키 안내** — 개발 키 만료(403) 시 Personal 앱 등록 안내 다이얼로그 + 도움말 개선 (개인 키는 24시간 만료 없음)


### v1.6.48

- **🖼 아수라장 실제 아이템 아이콘 복구** — 6슬롯 빌드가 한국어 이름을 다시 추측하지 않고 Blitz 원본 아이템 ID를 유지해 아수라장 전용 아이템까지 실제 아이콘으로 표시
- **💾 아이콘 캐시 연결 수정** — 백그라운드에서 받은 원본 이미지를 화면 스레드가 즉시 재사용해 글자 타일로 잘못 떨어지지 않도록 개선


### v1.6.47

- **🥈🥇💎 희귀도별 고정 TOP 3** — 챔피언마다 실버·골드·프리즘 증강을 각각 1~3순위 보드로 표시하고, 현재 제시 증강 평가는 별도로 분리
- **🎒 6슬롯 완성 빌드** — 신발을 포함한 아이템 6개를 3×2 카드로 표시하고, 데이터 누락 시 역할 기반 안전 빌드로 중복 없이 보충
- **🤖 아수라장 AI 작전 강화** — 승리 조건·증강 판단·6슬롯 선택 이유·한타 3단계·상대 핵심 대응 순서로 코칭
- **🛡 AI 아이템 검증** — 모델이 슬롯을 빼먹거나 같은 아이템을 반복하면 검증된 Blitz 빌드로 자동 교정


### v1.6.46

- **📅 주간 성장 리포트** — 이번 주와 이전 주의 승률·KDA·평균 데스·CS/분을 자동 비교
- **🎯 숙제 자동 채점** — 반복 데스 패턴으로 배정된 연습 목표를 이후 소환사의 협곡 경기에서 성공률로 정산
- **🔮 습관 스캐너** — 패배 후 20분 내 재큐·새벽 큐·35분 이상 장기전 성적을 표본 3판 이상일 때만 표시
- **🧬 4축 플레이 유형** — 교전·리스크·기복·파밍 점수와 4자리 유형 코드를 8판 이상 표본에서 진단
- **🖼 PNG 성장 카드** — 주간 지표와 숙제 달성률을 공유 가능한 이미지로 저장
- **🔔 상시 자동 복기** — 전적을 불러오면 게임 시작·종료 감지가 함께 켜지고, 양팀 챔피언 브리핑과 종료 복기가 매 게임 재무장
- **🛠 실패 피드백 보강** — AI 생성 준비와 자동 복기 화면 표시 실패를 로그·상태바·알림에 즉시 노출


### v1.6.45

- 🗺 복기 패널에 **킬·데스 지도** — 내 킬(파랑)·데스(빨강 X) 위치를 미니맵에 순서 번호와 함께 표시 (협곡·칼바람 모두)
- 미니맵 클릭 시 확대 지도 + **붕괴 스냅샷** — 30초 내 한 팀 3킬+로 판이 무너진 시점의 10인 위치와 챔피언 아이콘
- 타임라인 요약 수정 — 프레임 내장 이벤트를 읽도록 고쳐 첫 킬·오브젝트 타이밍·골드 격차 흐름이 실제로 표시됨


### v1.6.44 개선

- **🎯 아수라장 챔피언별 추천 동기화** — 173개 챔피언의 Blitz.gg 프리즘·골드·실버 증강 순서와
  코어 아이템을 앱에 반영
- **✅ 전체 parity 검증** — Master Yi를 포함한 전 챔피언 mismatch 0건을 자동 검증하는 validator와
  회귀 테스트 추가


### v1.6.43 개선

- **⏳ 게임 종료 자동 복기 안정화** — 전적 반영이 늦어도 이전 경기를 잘못 복기하지 않고
  「전적 업데이트 중」 상태에서 새 매치가 발행될 때까지 제한적으로 재확인
- **🔎 전적 신뢰도 표시** — 기존 전적 카드에 Riot 출처·패치·표본 수·데이터 시점과
  표본 부족 경고를 표시
- **🎯 반복 패턴 연습 목표** — 최근 5게임에서 충분히 반복된 데스 패턴을 한 가지 행동 목표로
  바꿔 표시 (승률 예측이나 인과관계 주장은 하지 않음)
- **🔕 게임 중 알림 대기** — 게임 중 토스트 알림은 중복 없이 대기하고, 게임 종료 뒤 한 번만 전달


### v1.6.42 개선

- **🌐 메타 데이터 복원력 강화** — blitz.gg 실시간 조회가 막히면 오래된 캐시 사용 시
  결과 상단에 「N시간 전 캐시 사용 중 (현재 패치와 다를 수 있음)」 경고를 표시하고,
  스크래핑 라이브러리 버전 고정을 풀어 차단 규칙 변경에 대응
- **🎯 밴픽 연동 안정화** — 클라이언트 lockfile이 남아 있어도 실제 연결이 안 되면
  조용히 실패하지 않고 명확한 안내를 표시 (LCU 헬스체크)
- **🧵 종료·스레드 안정성** — 앱 종료 중 워커 스레드가 파괴 중인 위젯에 접근하지
  않도록 차단하고, 진행 중인 스레드를 추적·정리
- **🪵 진단 로그 보강** — 아이콘·트렌드·스킨 재렌더 실패처럼 조용히 삼키던 오류를
  로그 파일에 기록해 문제 파악이 쉬워짐


### v1.6.41 개선

- **🔒 자동 업데이트 검증 필수화** — SHA256 사이드카가 없는 릴리스는 다운로드·실행하지 않고,
  설치 파일 크기 상한과 부분 다운로드 정리까지 적용
- **🌐 외부 통신 경계 강화** — 환경 프록시·사용자 CA 우회를 차단하고 Riot·Data Dragon·아이콘
  응답 크기와 JSON 형식, 동일 출처 HTTPS 리디렉션을 검증
- **🗝 비밀값 저장 위치 고정** — 설치본 API 키와 설정을 `%LOCALAPPDATA%\\롤실전코치`에만 저장해
  공유 또는 쓰기 가능한 실행 파일 폴더에 비밀값이 남지 않도록 개선


### v1.6.40 개선

- **🧭 전적 화면 경합 방지** — 리셋·새 로드 뒤 늦게 도착한 전적·아이콘·타임라인이
  현재 화면과 버튼 상태를 덮지 않도록 요청 세대별로 결과 소유권을 확인
- **🛡 입출력·업데이트 안전성 강화** — API 키 숨김 입력, CSV 수식 주입 방지,
  내보내기 경로 오류 안내, 업데이트 버전·응답 크기 검증과 LCU 경고 범위 격리
- **🌐 네트워크 복원력 보강** — Riot 429 비정상 대기값 방어, LLM 응답 스트리밍 상한,
  Riot 매치·타임라인 캐시 경로 통합 및 전체 회귀 테스트 220개 통과


### v1.6.39 개선

- **🛡 Blitz 메타 조회 안정화** — 호스트 검증·응답 크기 제한·리디렉션 제한과
  fresh/stale 디스크 캐시 폴백을 추가해 네트워크 실패 시에도 안전하게 복구
- **📦 Windows 배포 안정화** — frozen 빌드에서 ARAM 카탈로그 리소스를 보장하고,
  다른 모니터에 저장된 창 위치를 현재 화면 안으로 자동 보정
- **🧪 회귀 방지** — Python 3.10·3.11·3.12 CI matrix와 캐시·파서·패키징 테스트 보강


### v1.6.38 개선

- **🎨 티어 표시 정리** — blitz.gg 전환 후 빈 값으로 남던 `티어 ?` 제거
  - 구간(에메랄드+ 등) 표시와 함께 승률·픽·밴 요약으로 정리
- **✏️ 문서·표기 정리** — 사용자 노출 텍스트(API 안내 · --help · 오류 메시지 · AI 프롬프트)와
  내부 주석의 잔여 u.gg 언급을 blitz.gg로 일괄 정리


### v1.6.37 개선

- **🔁 메타 데이터 소스 전면 교체 — 전부 blitz.gg**
  - 협곡 빌드(룬/스킬/스펠/아이템/승·픽·밴률)를 **blitz.gg 단일 소스**로 전환 (u.gg 제거)
  - 카운터도 blitz.gg — **GD@15(Score) 데이터가 그대로 있어** 기존 분석 로직 무변경
  - Cloudflare 차단 없음 — 접속 안정성↑ · 파서 유지보수 1벌로 감소
  - ARAM/아수라장은 기존 Blitz 카탈로그 유지 (u.gg 폴백 제거)
  - CLI `meta`/`coach`도 blitz.gg 전환 (아수라장은 패키지 카탈로그)


### v1.6.36 개선

- **🐛 게임 시작 알림 안정화**
  - 네트워크 오류가 '게임 없음'으로 오인돼 알림이 두 번 울리던 문제 수정
  - 다른 계정으로 전적을 다시 로드해도 **옛 계정 기준으로 감지하던 문제** 수정 (계정 변경 시 watcher 재시작)
- **🐛 ARAM 증강 자동 입력 보강** — 리롤·늦게 도착한 증강도 감지 (챔피언+증강 기준 비교) · 숫자 ID 이름 필터링
- **🐛 스킨 변경 시 큐 필터 유지** — 스킨을 바꿔도 선택한 필터(솔랭/칼바람 등)가 유지됩니다


### v1.6.35 개선

- **🎮 게임 시작 알림** — 앱 실행 중 게임 시작을 감지해 **소리 + 상태바 + 미니 위젯**으로 브리핑
  - 내 챔피언·모드·참가 챔피언 자동 표시 · 설정에서 on/off (1분 간격 폴링, 가볍게 동작)
- **✨ ARAM 제시 증강 자동 입력** — 밴픽 중 LCU에서 **제시 증강을 자동으로 채움** (수동 붙여넣기 불필요)
- **📊 경기 흐름 차트** — 복기 타임라인에 **분당 골드 격차 · 내 CS · 데스** 시각화
- **🔀 내 전적 큐 필터** — 전체 / 솔랭 / 자유랭크 / 일반 / 칼바람 / 아수라장
- **🛡 GUI 실기동 스모크 테스트** — 앱 시작 크래시(v1.6.33) 재발 방지


### v1.6.34 개선

- **🐛 앱 시작 크래시 수정** — v1.6.33에서 타입 프로토콜이 런타임에 tkinter를 가리던 문제
  - `CoachAppAPI`(타입 전용) 스텁이 `after`·`destroy`·초기화를 섀도잉해 실행 직후 `RecursionError`로 종료되던 오류 해결
  - 프로토콜은 타입체크에만 쓰고 런타임 상속을 제거 — 창 생성·이벤트·토스트·종료 정상 동작 확인


### v1.6.33 개선

- **🐛 인앱 자동 업데이트 복구** — 새 버전 감지가 항상 조용히 실패하던 버그 수정
  - v1.6.8 모듈 분리 때 버전 비교 함수의 `self` 파라미터가 빠져 **업데이트 버튼이 활성화되지 않던** 문제 해결
  - 새 버전이 나오면 이제 정상적으로 버튼이 켜지고 상태바에 안내가 표시됩니다
- **⚡ 타입 안정성 완비** — mypy 전 항목 클린 (479 → 0)
  - 탭 믹스인 공용 API를 `CoachAppAPI` 프로토콜로 선언해 타입 검사 재개
  - 파서·아이콘·i18n의 타입 오류, 불필요한 `type: ignore` 9건 정리
- **🧹 내부 정리** — bs4 파서 str 강제 변환 · i18n 변수 섀도잉 제거 · CLI 설정 타입 보강


### v1.6.32 개선

- **🐛 SR 상세 분석 복구** — 상황템·체크리스트 섹션이 안 나오던 버그 수정
  - 적 조합 기반 **상황템 목록**과 **체크리스트**가 정상 표시됩니다
  - 전적 로드 시 자동 복기 설정의 폴백 경로도 함께 복구
- **🛡 화면 오류 안전망** — 창 내부 콜백 오류를 조용히 삼키지 않고 **상태바·로그**로 표시
- **🧹 내부 정리** — 중복 import 제거 · 린트 전 항목 클린 · 테스트 5개 추가 (전체 174개 통과)


### v1.6.31 개선

- **⚡ 스킨 즉시 적용** — 설정에서 스킨 누르면 **재시작 없이** 바로 반영
  - UI를 다시 그려 적용 · 설정 창은 다시 열려 연속 비교 가능
  - 전적 결과·탭 선택 유지

### v1.6.30 개선

- **🧹 스킨 UI 선·보더 정리** — 스킨 바꿔도 레이아웃이 들쭉날쭉하지 않게
  - 전 스킨 **공통 라운드/보더 두께** (카드 12·1, 입력 1)
  - 기본 Frame 보더 0 → 중첩 선·끊긴 선 방지
  - 카드 보더는 액센트 대신 **은은한 구분선** 색

### v1.6.29 개선

- **🎨 UI 스킨 11종** — 다크 7 + **밝은 4**
  - 다크: 클래식 · 네온 · 아쿠아 · 아이스 · 바이올렛 · 오션 · 민트
  - 밝은: **라이트** · **스카이** · **크림** · **블러시**
  - 설정에서 선택 → 앱 재시작 · 헤더 배지로 확인

### v1.6.28 개선

- **🎨 UI 스킨 7종** — 설정에서 선택 (재시작 적용)
  - **클래식** (골드) · **네온** · **아쿠아** · **아이스** · **바이올렛** · **오션** · **민트**
  - 헤더 배지로 현재 스킨 표시 · 언제든 클래식으로 복귀

### v1.6.27 개선

- **🎨 네온 스킨 차이 강화** — 시안 보더·버튼·탭 대비를 키워 클래식과 확실히 구분
  - 헤더에 **「네온 글래스」/「클래식」** 배지 표시
  - 설정에 저장 경로·재시작 안내 강화

### v1.6.26 개선

- **🎨 UI 스킨 2종 (되돌리기 가능)** — 설정 → UI 스킨
  - **클래식 (골드)** — 기존 스타일 그대로 보존 (`theme_classic.json`)
  - **네온 글래스 (실험)** — 레퍼런스 톤 (시안/퍼플 글래스 카드)
  - 선택 후 **앱 재시작** 시 적용 · 언제든 클래식으로 복귀

### v1.6.25 개선

- **🛡 AI 아이템 트리 후처리** — 모델이 템을 짧게 써도 최소 품질 보장
  - 한 줄에 몰린 `1코어…2코어…` **줄 분리**
  - 실명 코어가 **3개 미만**이면 메타 빌드로 빈 슬롯 보충 (AI가 쓴 슬롯은 덮지 않음)
  - 협곡 `coach_comp` · ARAM `coach_aram` 공통 적용

### v1.6.24 개선

- **⚙ 통합 설정 창** — 헤더 「⚙ 설정」
  - **AI 키·모델** · **종료 알림/자동 복기/로드 시 자동 복기** · **화면 배율** · **단축키 안내** · **Riot API 키**
  - 내 전적 탭은 Riot ID·전적 로드에 집중 (설정 UI 이동으로 결과 영역 확보)

### v1.6.23 개선

- **📺 ARAM 입력 접기 · AI 패리티** — 협곡(1.6.15)과 동일한 UX
  - 브리핑 후 입력란 **자동 접기**, 결과·AI 영역 최대화
  - 헤더 「입력 접기/펼치기」 수동 토글 · 초기화 시 입력 다시 펼침
  - AI 상세 코칭은 공용 큰 텍스트박스(17pt, 최대 640px) 그대로 사용

### v1.6.22 개선

- **🛑 게임 종료 시 자동 복기 on/off** — 알림(소리·플래시)과 분리
  - 「게임 종료 시 자동 복기 (패널 열기)」체크박스 (기본 ON)
  - 끄면 상태바만 갱신, 복기 패널은 안 열림 · `ui.json` 저장

### v1.6.21 개선

- **⏯ 전적 로드 시 자동 복기 옵션** — 「내 전적」탭 체크박스
  - **기본 OFF**: 로드 후 안내만 표시, 왼쪽 목록에서 직접 선택
  - ON이면 기존처럼 최근 1판을 바로 염 · `ui.json`에 저장
  - 게임 종료 후 자동 복기는 별도로 유지

### v1.6.20 개선

- **📋 내 전적 경기 목록 우선** — 트렌드·듀오에 가려 경기를 못 찾던 UX 수정
  - **최근 경기 버튼을 맨 위**에 배치 (헤더 바로 아래)
  - 트렌드·듀오는 **「▼ 트렌드·듀오 보기」** 로 접기 (기본 접힘, 펼친 상태 기억)

### v1.6.19 개선

- **↩ 경기 복기 뒤로가기** — 복기 패널 상단에 **「← 목록」·이전/다음** 버튼
  - 목록으로 돌아가 다른 경기 선택 · `1/15` 위치 표시 · 선택 경기 왼쪽 목록 강조

### v1.6.18 개선

- **🔔 게임 종료 알림 on/off** — 「내 전적」탭에서 **소리·작업표시줄 깜빡임**을 끌 수 있음
  - 꺼도 상태바 문구와 자동 복기는 유지 · `ui.json`에 저장되어 재실행 후에도 유지

### v1.6.17 개선

- **🐛 ARAM 메이헴 크래시 수정** — `MayhemCoach`에 없는 `analyze` 호출로
  아수라장 브리핑이 실패하던 문제 (`advise` 호출 + 호환 별칭)

### v1.6.16 개선

- **🛡 AI 아이템 트리 1~5코어** — AI 코칭이 1~2코어만 말하던 문제 수정
  - 메타 코어 + 상황템을 합쳐 **최대 5코어 슬롯**을 프롬프트에 전달
  - AI에 **1~5코어를 각각 한 줄**로 쓰도록 강제 (3코어는 거의 필수, 4·5는 상황/후반)
  - 체크리스트·위젯 요약·규칙 기반 조언도 1~5코어 루트 표시로 통일
  - AI 상세 박스 높이 여유 확대 (최대 640px)

### v1.6.15 개선

- **📺 상세 코칭 최대화** — 분석 후 입력란 **자동 접기**, 결과 영역 거의 전체 사용
  - AI 상세 코칭을 **큰 글씨(17pt) 텍스트 박스**(높이 280~520px)로 표시
  - 헤더 「입력 접기/펼치기」 버튼으로 수동 토글

### v1.6.14 개선

- **👁 결과·AI 코칭 가독성** — 협곡 입력 패널을 콤팩트하게 줄여 결과 영역 확대
  - AI 카드: **상세 코칭을 먼저·큰 글씨**로, 핵심 요약은 아래 작은 글씨
  - 카운터 행·섹션 간격 축소로 스크롤 부담 감소

### v1.6.13 개선

- **🐛 실행 크래시 수정** — `MeTabMixin._profile_labels` 에 빠진 `self` 인자로 앱이 시작 직후 종료되던 문제 수정

### v1.6.12 개선

- **🔍 화면 배율** — 헤더 드롭다운(0.9~1.2x)으로 글자·UI 크기 조절, `ui.json`에 저장
- **🚫 LCU 밴 반영** — 밴픽 연동 시 이미 밴된 챔프는 밴 추천 목록에서 뒤로 +「이미 밴됨」표시
- **⚔ ARAM 조합 카드** — 인게임 자동검색 후 브리핑에 적/아군 태그 기반 위협·시너지 요약

### v1.6.11 개선

- **🖼 트렌드 시각화** — 최근 승패 W/L 칩 + KDA 막대 미니 차트 (내 전적)
- **👥 듀오 통계** — 같이 뛴 소환사 승률·판 수 집계 (2판 이상)
- **🌐 전역 핫키** — Windows에서 `Ctrl+Shift+W` 가 게임 포커스 중에도 위젯 토글 (등록 실패 시 앱 내 단축키만)

### v1.6.10 개선

- **📈 최근 트렌드** — 내 전적 탭에 최근 승률 추세·연승/연패·평균 데스·KDA·CS@10 요약 카드
- **🚫 밴 추천** — 내 챔프를 입력하면 그 챔프를 카운터하는 픽을 밴 후보로 표시 (빠른 추천·상세 분석)
- **⌨️ 미니 위젯 단축키** — `Ctrl+Shift+W` 로 위젯 토글, 위치 기억

### v1.6.9 개선

- **💬 비모달 토스트** — 입력 오류·LCU 실패·복사/내보내기 완료 등을 상태바+하단 토스트로 안내 (인게임 모달 차단 완화)
  - 삭제·업데이트·모드 확인 등 의사결정은 기존 확인 창 유지
- **🧭 에러 문구** — Riot 401/403/404/429·LCU·Cloudflare를 조치 가능한 한국어로 정규화 (`gui/errors.py`)
- **🖼 아이콘 프리페치** — 챔프/아이템 중복 제거 후 필요 크기만 다운로드, 다른 form 로드 시 재렌더 스킵

### v1.6.8 개선

- **🧱 GUI 모듈 분리** — `app.py` 신 클래스(~3.5k줄)를 탭/역할 믹스인으로 분해
  - `sr_tab` · `aram_tab` · `me_tab` · `ai_mixin` · `live_mixin` · `update_mixin` · `constants`
  - 동작은 동일, 이후 탭별 수정·테스트가 쉬워짐

### v1.6.7 개선

- **🛡 안정성·보안** — AI 코칭 generation-id로 늦은 응답 덮어쓰기 방지, 결과 히스토리는 UI 스레드에서만 갱신, 자동 업데이트 **SHA256 검증** (`.sha256` 사이드카)
- **🔑 키·서버 UX** — 기본 플랫폼 `kr`, Development 키 저장 시각·24h 만료 안내, opencode 키 경로 Windows/Linux 다중 탐색
- **📦 캐시·데이터** — 공통 `cache_root()`, blitz.gg 디스크 캐시 72h + 네트워크 실패 시 stale 폴백, 매치 캐시 prune 10분 스로틀
- **🔍 LCU** — lockfile 탐색 경로·레지스트리 보강, 상태바 오타 수정
- **🧰 유지보수** — 로그 핸들러 중복 제거·로테이션, User-Agent 버전 연동, `gui/updater`·`gui/ai_text` 분리, AGENTS 릴리스 예외 규칙

### v1.6.6 개선

- **🧹 화면 인식(베타) 제거** — ARAM 탭의 화면 캡처 증강 인식 기능·`mss`/`numpy` 선택 의존성·관련 테스트/문서를 삭제
  - 증강 입력은 **수동 입력 · 증강 목록 피커 · LCU 밴픽**만 사용
  - 설치본/개발본 모두 미지원 기능 안내가 더 이상 뜨지 않음

### v1.6.5 개선

- **🧭 ARAM 종료 탭 유지** — 게임이 끝나도 현재 보고 있던 ARAM 탭을 `내 전적`으로 강제 전환하지 않음
- **🤖 AI 코칭 가독성 강화** — 골드 테두리·큰 제목·큰 본문으로 AI 카드를 강조하고, 핵심 요약을 카드 맨 위에 별도 표시
  - 미니 위젯에도 AI 전체 응답 대신 우선순위가 높은 핵심 포인트만 요약

### v1.6.4 개선

- **🇰🇷 Blitz ARAM 아수라장 카탈로그** — Blitz의 현재 패치 게임 데이터에서 231개 증강의 공식 한글명·설명·희귀도·티어를 다시 수집
  - 모든 증강 설명을 u.gg가 아닌 Blitz 한국어 데이터 기준으로 통일
  - 모든 증강 아이콘을 Blitz CDN에서 검증해 카드와 아이콘 캐시에 반영
  - 전 챔피언 173명의 Blitz ARAM-Mayhem 완성 아이템 코어 순서와 챔피언별 빌드 링크를 패키지
  - ARAM 결과의 출처와 패치 표시도 Blitz 카탈로그 기준으로 고정하고, u.gg는 Blitz 데이터 누락 시에만 폴백

### v1.6.3 개선

- **📅 현재 패치 기준 AI 코칭** — 오늘 날짜와 분석 패치를 프롬프트에 고정해 이전 시즌 수치·메타를 현재 내용처럼 단정하지 않도록 강화
  - 협곡 상세 정보에 적 정글·서폿·탑·미드·원딜과 내 챔피언이 모두 입력되면 상대 5인 조합에 맞춘 한타·진입·보호·오브젝트 운영을 우선 코칭
  - AI 코칭 카드가 결과 맨 위에 표시되어 긴 결과를 스크롤하지 않아도 바로 확인
- **🔮 ARAM 아수라장 실전 코칭** — 실행 중 게임 자동 검색으로 저장한 우리 조합·상대 조합을 AI에 함께 전달
  - 조합에 맞는 플레이 방식, 제시 증강 우선순위, ARAM 아이템 빌드를 한 번에 안내
  - 규칙 기반 u.gg/Data Dragon 결과와 함께 현재 패치 기준으로 표시
- **🔑 AI 설정 분리** — 내 전적 탭에서 API 키(마스킹 입력/저장)와 AI 모델을 별도 행으로 설정
  - `deepseek-v4-flash`, `deepseek-v4-pro`, `kimi-k3`, `glm-5`, `qwen3.7-plus` 선택 지원
- **🛡 게이트웨이 장애 폴백** — 500 응답·타임아웃을 3회 재시도하고, 실패하면 규칙 기반 결과를 유지해 화면이 멈추지 않음

### v1.6.2 개선

- **🧠 AI 모델 선택** — 내 전적 탭 드롭다운으로 모델 변경 (기본 `deepseek-v4-flash` · `deepseek-v4-pro` · `kimi-k3` · `glm-5` · `qwen3.7-plus` — 모두 opencode-go 게이트웨이 동작 확인)
- **📌 미니 위젯 AI 코칭** — AI 코칭 결과가 미니 위젯에 바로 표시 — 결과 화면을 스크롤하지 않아도 게임 위에서 확인

### v1.6.1 새 기능

- **🤖 선택형 AI 코칭** — 규칙 기반 결과 위에 진짜 자연어 코칭을 얹습니다
  - opencode-go 게이트웨이(`deepseek-v4-flash`) 연동 — 빠른 추천/상세 분석/경기 복기 결과마다 **AI 코칭 카드** 추가
  - **키 자동 감지**: opencode CLI 인증 파일을 같은 PC에서 자동으로 찾음 → 별도 설정 없이 바로 활성
  - **수동 입력도 가능**: 내 전적 탭의 「AI 코칭 키」 칸에 붙여넣기 (비우면 자동 감지)
  - 키가 없거나 생성 실패해도 **기존 규칙 기반 결과로 폴백** — 기능은 그대로 동작
  - 비용 부담 없음 (요청당 수백~수천 토큰, 개인용량 수준) · 키는 저장소에 절대 포함되지 않음

### v1.6 새 기능

- **🎨 GUI 전면 리디자인 (다크 리그 에디션)** — 기능은 그대로, 디자인만 새로
  - LoL 클라이언트 스타일 **딥 네이비 + 골드 팔레트** 커스텀 테마 (기본 블루 → 골드 `#C8AA6E` 액센트)
  - 섹션 헤더 **골드 액센트 바**, 카드형 결과 행(보더+라운드), **S/A/B/C 등급 칩** (카운터 GD@15·증강 티어)
  - 탭 선택 골드 강조 + 버튼 변형 체계(골드/패널/초록/보라), 입력칸·드롭다운·스크롤바 전면 정돈
  - 미니 위젯 — 골드 상단 라인 + 카드 바디
- **🧪 기능 회귀 없음** — GUI 테스트 포함 전체 pytest 통과 (디자인 토큰 문서: `docs/concepts/`)

### v1.5 새 기능

- **⬆ 자동 업데이트 알림** — 앱 시작 시 GitHub 최신 릴리스 확인, 새 버전이 있으면 상태바에 안내
- **📈 타임라인 복기** — 경기 복기에 15분 내 골드 · 첫 킬/포탑 · 용/바론/전령/공허유충 처치 타이밍 추가 (Match V5 Timeline)
- **📌 미니 위젯 개선** — 위젯 클릭 시 메인 창 복귀 + 📋 복사 버튼 (인게임에서 바로 활용)
- **🖼 아이콘 버전 영속화** — Data Dragon 버전을 캐시에 저장, 오프라인에도 마지막 성공 버전 사용 (신규 챔피언 아이콘 보존)
- **🧪 GUI 테스트 안정화** — Tk 이미지 네임스페이스 충돌 제거로 flaky 테스트 수정
- **📦 빌드 안정화** — PyInstaller hiddenimports 보강 (함수 내 import 모듈 누락 방지)

### v1.5.1 개선

- **📈 타임라인 디스크 캐시** — 복기 타임라인도 `cache/timelines/`에 저장, 재조회 시 API 재호출 없음 (정리 규칙 포함)
- **🛡 단일 인스턴스** — exe 중복 실행 시 "이미 실행 중" 경고 후 종료 (LCU/워처 중복 폴링 방지)
- **🔢 경기 수 선택** — 전적 탭에서 최근 5~50경기 드롭다운으로 선택 (기존 15경기 고정)
- **🧹 빌드 정리** — 빈 루트 `data/` 참조 제거 (패키징은 `src/lol_coach/data`만)

### v1.5.2 개선

- **🗑 프로필 삭제 버튼** — 전적 탭에서 저장된 프로필을 목록에서 바로 제거 (Riot 계정·전적엔 영향 없음)
- **📄 로그 파일 저장** — `logs/lol_coach.log`에 기록 (설치본 문제 진단 시 이 파일 공유)
- **🪟 창 크기/위치 기억** — 닫을 때 `ui.json`에 저장, 다음 실행 시 복원
- **🔁 릴리스 안정화** — 테스트가 Windows Tk 일시 오류로 실패해도 1회 재시도 (릴리스 중단 방지)

### v1.5.3 개선

- **🧹 탭별 초기화 버튼** — 협곡/ARAM/전적 탭에서 입력·결과를 한 번에 비움 (LCU 밴픽 추적도 함께 중단)

### v1.5.4 개선

- **🔄 앱 내 자동 업데이트** — 업데이트 버튼 한 번으로 최신 인스톨러 다운로드 → 무음 설치 → 새 버전 자동 재실행 (설정·캐시·프로필 유지, 캐시 재사용)

### v1.5.5 개선

- **🔑 API 키 보호** — 전적 탭 초기화가 API 키 입력칸을 비우지 않음 (.env 키 유지)
- **🔧 전적 로드 버튼 잠김 수정** — 초기화 후에도 로드 버튼 정상 동작 (자동 로드와의 busy 경합 해소), 로드 중 클릭 시 상태바 안내

### v1.5.6 개선

- **⚡ 전적 로드 즉시 표시** — 아이콘 수백 개 다운로드가 끝날 때까지 기다리지 않고, 데이터를 먼저 렌더링 (아이콘은 백그라운드 프리페치 후 채워짐)
### v1.4 새 기능

- **🎯 LCU 밴픽 연동** — 게임 클라이언트 로컬 API로 **챔피언 셀렉트 중** 적/내 픽을 읽어 바로 카운터 추천 (API 키 불필요, Spectator로 안 되던 밴픽 단계 커버)
- **🔔 게임 종료 자동 복기** — 인게임 자동입력 후 종료를 폴당해 방금 판 복기를 자동 표시
- **🏅 랭크 표시** — League V4 솔로/자유 랭크 티어·LP·승률 (GUI 내 전적 탭 + CLI `profile`)
- **📊 챔피언 풀 진단** — 베이지안 보정 승률 기반 집중/유지/정리 추천 (GUI + CLI `pool`)
- **📌 미니 위젯** — 마지막 분석 요약을 항상 위 창으로 (인게임 중 Alt+Tab 불필요)
- **📁 전적 내보내기** — CSV/JSON 파일로 저장 (GUI 버튼 + CLI `export`)
- **👤 멀티 프로필** — 여러 Riot ID 저장·전환 (`profiles.json`)
- **💡 아이템 툴팁** — 상세 분석 아이템에 마우스를 올리면 가격·설명 표시
- **⚡ 성능** — 매치 상세 병렬 조회 + 디스크 캐시(`cache/matches/`)로 재조회 즉시 완료
- **🔧 로깅** — CLI `-v/--verbose` 또는 `LOL_COACH_DEBUG=1`로 네트워크 진단 로그
- **⌨️ 챔피언 자동완성** — 협곡 탭 7개 입력(적 라이너·내 챔프·정글·서폿·탑·미드·원딜) 한글/영문 자동완성 (Windows IME 조합 문자 지원)
- **🔄 LCU 밴픽 자동 추적** — 밴픽 중 픽이 바뀔 때마다 4초 간격 자동 갱신, ARAM 밴픽은 협곡 탭에서 눌러도 탭 자동 전환
- **🗂 탭별 동시 작업** — 협곡 분석 중에도 전적 로드·ARAM 브리핑 실행 가능 (글로벌 잠금 → 작업별 잠금)
- **📜 결과 히스토리 + 📋 복사** — 최근 20개 결과 복원 버튼, 요약 클립보드 복사 버튼
- **🗂 증강 목록 피커** — 카탈로그 200+종 검색 팝업에서 클릭으로 제시 증강 입력
- **⚡ 시작 시 자동 로드** — 마지막 프로필 전적 자동 로드 + 서버 선택 드롭다운
- **💾 blitz.gg 디스크 캐시** — 빌드 데이터 영속 저장, 재시작·일시 차단에도 마지막 빌드 복원
- **🔔 종료 알림** — 게임 종료 시 비프음 + 작업표시줄 플래시, 폴링 간격 45초→15초 단축
- **🧹 캐시 자동 정리** — 매치 캐시 30일 경과분 + 1000개 초과분 자동 삭제
- **⏰ API 키 만료 안내** — 401/403 응답 시 "키 재발급" 안내 (개발 키 24시간 만료)

### Modes

| Mode | Riot queues | blitz.gg source |
|------|-------------|-----------------|
| `summoners_rift` | 400/420/430/440… | `/lol/champions/{champ}/build?role={ROLE}` |
| `aram` | 450 ARAM + **2400 ARAM Mayhem** | 패키지된 Blitz 카탈로그 (`blitz_aram_builds.json`) |

## Project layout

```
lol-coach/
├── main.py                 # CLI entry
├── gui_main.py             # GUI entry
├── requirements.txt
├── .env.example
├── src/lol_coach/
│   ├── cli.py              # click commands (setup/profile/live/meta/coach/pool/export)
│   ├── config.py           # API key / .env / 멀티 프로필(profiles.json)
│   ├── lcu.py              # LCU lockfile + 챔피언 셀렉트 파싱
│   ├── log.py              # 공통 로깅 (-v/--verbose)
│   ├── riot/               # Account, Match V5(+병렬/캐시), League V4, Spectator
│   ├── blitz/              # blitz.gg fetch + HTML parse (빌드/카운터)
│   ├── static/             # Data Dragon / 로컬라이저 / 아이콘·증강 카탈로그
│   ├── analysis/           # coach · 복기 · 조합 · 아수라장 · 풀 진단 · 내보내기
│   └── gui/                # app · 자동완성 · 미니 위젯 · 툴팁 · 종료 감지 워처
└── tests/
```

## GUI (롤 실전 코치)

```powershell
cd C:\Users\hyj53\lol-coach
$env:PYTHONPATH = "src"
pip install customtkinter
python gui_main.py
```

| 탭 | 기능 |
|----|------|
| **소환사의 협곡** | 적 라이너(+정글/서폿/전체) → 카운터 · 조합 위협 · 용/바론 · 코어+상황템 · 체크리스트 · **LCU 밴픽 불러오기** · 아이템 툴팁 |
| **ARAM 아수라장** | 챔피언 + **게임에서 제시된 증강** → Top5 추천 · 피할 증강 · 칼바람 빌드 · 실전 팁 · **LCU 내 픽 입력** · **증강 목록 피커** |
| **내 전적** | 내 Riot ID 연동 · 최근 경기 · 챔프별 성적 · **랭크** · **챔프 풀 진단** · **CSV/JSON 내보내기** · **멀티 프로필** · **게임 종료 자동 복기** |

공통: 헤더의 **📌 미니 위젯**으로 마지막 분석 요약을 항상 위 창에 띄울 수 있습니다.

## LCU 밴픽 연동 (신규)

GUI 각 탭의 **🎯 밴픽 (LCU)** 버튼은 게임 클라이언트의 로컬 API(lockfile)를 읽어
**챔피언 셀렉트 중인 픽/밴**을 가져옵니다.

- Riot API 키 없이 동작 (로컬호스트 통신만 사용)
- 협곡 탭: 적 5명 픽 → 포지션 추정(태그 기반, 부정확할 수 있음) + 내 픽/포지션 자동 입력 → 즉시 카운터 추천
- ARAM 탭: 내가 고른 챔피언 자동 입력
- 클라이언트가 꺼져 있거나 밴픽 중이 아니면 안내 메시지만 표시
- lockfile 경로가 기본값(`C:\Riot Games\League of Legends\lockfile`)과 다른 경우
  환경변수 `LOL_LOCKFILE`로 지정

## Setup

```powershell
cd lol-coach
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pip install -e .
```

`pyproject.toml` 없이 바로 쓰려면:

```powershell
$env:PYTHONPATH = "src"
python main.py setup
```

### Riot API Key

1. https://developer.riotgames.com/ 에서 Development API Key 발급
2. 아래 중 하나:

```powershell
python main.py setup
# 또는
python main.py setup --api-key "RGAPI-...." --riot-id "소환사명#KR1" --platform kr
```

키는 프로젝트 루트 `.env`에 저장됩니다. **개인 키는 24시간마다 만료**됩니다.

## Commands

| Command | Description |
|---------|-------------|
| `setup` | API 키 / 기본 소환사 설정 |
| `test-key` | 키 유효성 검사 |
| `profile` | 최근 전적 분석 (+ 랭크 표시) |
| `live` | 현재 인게임 여부 |
| `meta <champ> -r mid` | blitz.gg 메타 빌드만 |
| `coach <champ> -r mid` | 메타 + 내 기록 맞춤 코칭 |
| `pool` | 챔피언 풀 진단 (집중/유지/정리) |
| `export` | 최근 전적 CSV/JSON 내보내기 |
| `gui` | 데스크탑 GUI 실행 |

모든 명령은 `-v/--verbose`로 디버그 로그를 켤 수 있습니다 (`lol-coach -v profile`).

### Examples

```powershell
$env:PYTHONPATH = "src"

python main.py setup --api-key "RGAPI-xxx" --riot-id "소환사명#KR1" --platform kr
python main.py test-key
python main.py profile --count 15
python main.py live
python main.py meta Ahri -r mid
python main.py meta Ahri -m aram
python main.py coach Ahri -r mid --lookback 20
python main.py coach Ahri -m aram --lookback 20

# ARAM Mayhem: GUI의 "제시 증강" 칸에 게임에서 보이는 증강을 쉼표 또는 줄바꿈으로 직접 입력
# CLI의 `-m aram`은 패키징된 아수라장(ARAM Mayhem) 메타 카탈로그를 조회합니다
# (네트워크 없이 동작; 제시 증강 기반 브리핑은 GUI에서 제공).
```


## How blitz.gg data is fetched

1. `cloudscraper`로 챔피언 빌드 페이지 HTML을 가져옵니다 (허용된 blitz.gg 호스트만 접근).
2. BeautifulSoup으로 **현재 패치, 승/픽/밴률, 룬(rune-img.active), 스킬 우선순위, 소환사 주문(CDN id), 코어/시추에이셔널 섹션**을 파싱합니다.
3. 결과는 메모리 5분 + 디스크 72h 캐시됩니다 (네트워크 실패 시 stale 폴백).

> 아이템은 완성/시추에이셔널 그룹을 사용하고, 신발은 별도 boots 섹션으로 분리합니다.

## ARAM Mayhem 코칭

### 입력

- 사용자가 **게임 안에서 실제로 제시된 증강 이름**을 쉼표/줄바꿈으로 입력합니다.
- 제시되지 않은 증강은 추천·회피에 포함되지 않습니다.
- 한글/영문/별칭을 모두 인식하며, 중복과 카탈로그에 없는 이름은 별도로 안내합니다.

### 개인화 로직

- 챔피언 성향(`Mage`, `Marksman`, `Assassin`, `Fighter`, `Tank`, `Support`)을 Data Dragon 태그로 판단합니다.
- 제시된 증강 중 **S/A 등급 + 챔프 성향 시너지**를 우선 추천하고, 성향 충돌이나 B등급은 회피로 분류합니다.
- 아이템 빌드는 패키지된 **Blitz 카탈로그**를 사용하고 누락 시 클래식 ARAM 폴백을 제공합니다.

### 출처

- 증강 데이터: 패키징된 `src/lol_coach/data/aram_mayhem_augments.json` 기준.
- 아이템/스킬 빌드: 패키지된 Blitz 카탈로그 (`blitz_aram_builds.json`).
- 팁: Data Dragon 스킬 정보 + 제시된 증강 위주의 조합 해석.

### 이미지

- 증강 아이콘은 **카탈로그에 등록된 정확한 후보 URL**만 사용합니다.
- 우선순위: Riot Data Dragon / Riot 패치 노트 / blitz.gg / League Wiki / ARAM Mayhem 커뮤니티(arammayhem.com).
- 네트워크 실패·검증 실패 시 이미지 대신 **이름+등급 카드**를 표시합니다.

## Cache

- 챔피언·아이템·증강 아이콘은 Data Dragon/blitz.gg/커뮤니티에서 받아 **로컬에 캐시**합니다.
- **매치 데이터도 디스크 캐시**(`cache\matches\{match_id}.json`) — 매치 payload는 불변이라
  한 번 받은 경기는 API 재호출 없이 즉시 로드됩니다. (`RiotClient(use_cache=False)`로 해제 가능)
- 캐시 위치: 설치본은 `%LOCALAPPDATA%\롤실전코치\cache\`, 포터블/개발 환경은 exe/프로젝트 옆 `cache\`.
- GUI는 브리핑 생성 시 백그라운드 스레드에서 필요한 아이콘을 프리패치합니다.
- 이전에 성공한 이미지 URL은 `last_known_good` 인덱스로 유지되어, 오프라인/일시 장애 시에도 마지막으로 확인된 아이콘을 보여줍니다.
- 이미지 파일이 깨지면 다음 요청 때 자동으로 다시 받습니다.

## Maintainer check

릴리스 전 카탈로그 상태를 점검하려면:

```powershell
python scripts/refresh_aram_mayhem_data.py
```

blitz.gg HTML 구조가 바뀌어 파싱이 깨졌는지 확인하려면:

- `tests/test_parser.py`의 인라인 샘플(`tests/blitz_samples.py`)이 실페이지 구조와 어긋나면
  파싱 회귀가 발생한 것이므로, 실페이지에서 샘플을 다시 캡처해 갱신합니다.
- GUI에서 빌드/카운터 로드가 실패하면 상태바에 오류가 표시되고, 캐시가 있으면 stale 폴백이 동작합니다.

- `data/aram_mayhem_augments.json`의 스키마·중복·ID·등급 유효성을 검사합니다.
- 모든 증강에 **≥128 px 후보 이미지**가 있는지 확인합니다.
- `--patch 15.x`로 기대 패치를 지정할 수 있습니다.
- `--require-full-coverage` 기본값은 `true`: 이미지 후보가 없는 증강이 있으면 비제로 종료합니다.
- `--allow-community-only` 기본값은 `true`: Riot/Wiki 자동 검증이 어려울 때 arammayhem.com 커뮤니티 자산을 정확한 후보로 허용합니다.

이 스크립트는 **이미지 URL을 새로 만들지 않습니다**. 기존 카탈로그의 내보내기 출처와 해상도 제약만 검증합니다.

## Notes

- 기본 플랫폼: `na1` / routing `americas`
- Rate limit(429) 시 `Retry-After`를 존중합니다.
- blitz.gg 이용약관에 유의하세요. 개인/학습 용도로만 사용하세요.

## License

MIT (personal project)
