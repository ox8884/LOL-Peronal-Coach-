# LoL Personal Coach CLI

Riot Match API로 최근 전적을 분석하고, [blitz.gg](https://blitz.gg) 현재 패치 메타 빌드(룬/스킬/아이템/승률)를 가져와 **맞춤 코칭**을 출력하는 Python CLI/GUI입니다.


[![Release](https://img.shields.io/badge/릴리스-latest-3B8ED0?logo=github)](https://github.com/ox8884/LOL-Peronal-Coach-/releases/latest)
[![Download](https://img.shields.io/badge/⬇%20인스톨러%20다운로드-Releases-81C784)](https://github.com/ox8884/LOL-Peronal-Coach-/releases/latest)

> **설치 파일은 [Releases](https://github.com/ox8884/LOL-Peronal-Coach-/releases/latest) 페이지에서 받을 수 있습니다.**
> `.env`(API 키)는 설치 파일에 포함되지 않으며, 첫 실행 시 입력하면 자동 생성됩니다.
> 📄 **기능 소개 페이지**: [**롤 실전 코치 — 기능 가이드**](https://ox8884.github.io/LOL-Peronal-Coach-/features.html) — 브라우저에서 바로 열어 보는 전체 기능 가이드 (GitHub Pages 렌더링)

## Features

1. **Riot ID → PUUID + 최근 N게임 분석** (KDA, CS, 승률, 포지션, 챔프별 성적, **모드별 구분**)
2. **현재 게임 중인지 확인** (Spectator V5)
3. **챔피언 메타 빌드** — 협곡·칼바람·아수라장 전부 **blitz.gg** 단일 소스
4. **내 최근 해당 챔프 플레이 vs 메타 비교** + 자연어 조언 (`--mode aram` 지원)
5. **선택형 AI 코칭** — 프로바이더 선택(opencode-go / Gemini / Groq / OpenRouter), 현재 패치 기준 조합·ARAM 코칭

### v1.6.113

- **🔍 경량화 전수 감사 (측정 기반) — 시작 무게·메모리 상한·유휴 CPU**
  - **부팅 임포트 경로에서 cloudscraper·bs4·카탈로그(패키지 JSON 556KB) 전부 제거** — 첫 blitz 파싱/첫 브리핑 때 로드하도록 지연. 창 뜨기 전 동기 작업 최소화
  - **메모리 체류 상한 정비**: 매치/타임라인 메모리 LRU 128→**24** + 2MB 초과 payload는 디스크 전용 (최악 체류 수십~100MB → 상한 보장), 아이콘 PIL 캐시 1024 상한 추가
  - 실측: 파이썬 기본 15.9MB → 앱 임포트 51.7MB → 데이터 로드 58.1MB / 유휴(클라이언트 없음) 폴러 CPU **0.0%** / 디스크 캐시는 전부 상한·프루닝 확인 (matches 86MB→1000파일 30일 자동 정리, 로그 2MB×4 로테이션)
  - 제외 판정 기록: LCU websocket(폴링이 이미 루프백 GET 1회/3~6초로 충분히 싸고, 핵심 기능 회귀 리스크 > 이득), 스크아팅 병렬화(Riot 개발키 20 req/s 상한 도달 — 429 유발)

### v1.6.112

- **🤖 AI 코칭 실시간 스트리밍 — "생성 중…" 10~30초 정적 대기 제거**
  - LLM 응답을 **SSE 스트리밍**으로 받아 첫 토큰부터 AI 카드에 점진 표시 — 체감 대기가 전체 생성 시간(10~30초)에서 **첫 토큰 도착(1~2초)** 으로 단축
  - 생성 중 로딩 라벨에 최신 문장이 티커로 흐르고, 완료 시 기존처럼 정돈된 카드로 교체 (150ms 스로틀 — UI 이벤트 큐 보호)
  - 게이트웨이가 스트리밍을 무시하고 JSON으로 답하면 자동 폴백, 연결이 중간에 끊기면 지금까지 받은 텍스트라도 반환
- **⚡ 추가 성능 정비 (코드 리뷰 후속)**
  - **매치/타임라인 캐시 메모리 LRU(128)** 추가 — 첫 브리핑 때 디스크에서 ~100매치 × 수MB JSON을 재파싱하며 UI가 떨리던 것 제거
  - **LLM 세션 재사용** — 호출마다 새 TLS 연결을 만들던 것 제거 + 429/5xx 재시도가 **Retry-After 헤더 존중** (기존 5초 만에 3회 소진 → 한도 초과 악화 방지), `probe_gateway` 응답 누수 수정
  - blitz.gg **스펠 파싱을 스펠 이미지 스코프로 축소** — 문서 전체 수천 개 `<img>` 순회 제거 (실제 페이지 스냅샷으로 결과 동등성 검증)
  - 내 전적 탭 — **아이콘 프리페치 후 새로 받은 아이콘이 있을 때만 재렌더** (두 번째 로드부터 전체 화면 재빌드 생략)

### v1.6.111

- **⚡ 전면 성능 최적화 — "blitz.gg보다 빠르고 가볍게" 코드 리뷰 기반 정비**
  - **게임 중 폴링 경량화**: Live Client 폴링(3초)이 매번 새 TLS 연결을 만들던 것을 세션 재사용으로 제거 + 워커 스레드에서 데이터 변화 감지 → 변한 폴만 메인 스레드로 전달 (게임 내내 초당 발생하던 불필요한 콜백·로그·챔피언 해석 제거)
  - **부팅·정적 데이터**: Data Dragon ~9MB JSON에 메모리 캐시 레이어 추가 — 부팅 후 로컬라이저가 같은 팩을 **다시 파싱하던 198ms 제거 (0.7ms)**, 실행 중 재파싱 전부 제거
  - **자동완성·브리핑 CPU**: 챔피언 검색 키(~1,700개)를 로드 시 1회 사전계산 — 매 호출 정규식 ~3,400회 제거, `search_champions` **6배 빠름 (0.19ms→0.03ms)** + 열린 팝업에서 같은 쿼리 재검색 단락회로
  - **아이콘 렌더**: CTkImage 캐시 도입 — 카드 1장에 아이콘마다 PIL 복사 2회+PhotoImage 재생성하던 것 제거 (전적 50판·티어표 173칩 렌더가 수백 ms씩 절약) / 증강 아이콘도 메모리 캐시 추가 (보드 렌더당 디스크 디코드 ~19회 제거)
  - **상시 밴픽 폴러**: 밴픽이 아닐 때 폴당 요청 3회+lockfile 재스캔 → **요청 1회**로 축소 (세션 캐시 유지 + lockfile 부정캐시 15초 + 404 즉시 반환)
  - **캐시 정책**: blitz 메모리 캐시 LRU 상한(64) 도입 + 아수라장 정적 데이터(패치 불변)는 메모리 TTL 72h 적용 — 장시간 게임 중 반복 디스크 재독기 제거
  - 기타: `advise()` 중복 증강 변환 제거, ui.json mtime 캐시 (폴러가 매 폴 파일을 재파싱하던 것 제거), `champion_detail` 캐시 키 불일치 버그 수정 + 스킨/로어 대형 필드 트리밍, 아이템명→ID 역인덱스 (브리핑마다 3,500개 전체 스캔 ×6~12회 → dict 조회)

### v1.6.110

- **⚡ 아수라장 첫 브리핑 지연 제거 (부팅 프리페치 + 병렬 조회)**
  - 부팅 직후 백그라운드로 챔피언 목록·증강 게임데이터를 미리 받아둠 — 첫 브리핑 때 수백 KB가 순차 걸리던 것 제거
  - **증강 티어 + 사이트 빌드 순서를 병렬 조회** (기존 순차) — 조회 지연 약 절반
- **🏆 티어표 챔피언 아이콘 누락 수정 (닐라·렉사이)** — 티어표가 캐시에 있는 아이콘만 표시하고 다운로드를 하지 않아, 캐시에 없던 챔피언 2개가 글자 placeholder로 나오던 문제. 이제 페이지 로드 시 워커 스레드에서 **173챔피언 아이콘을 전부 미리 받은 뒤** 렌더 (한 번 받으면 영구 캐시)

### v1.6.109

- **🎯 아수라장 아이템 순서를 blitz.gg 사이트와 완전히 동일하게 수정**
  - **근본 원인**: blitz.gg가 보내는 HTML에 charset이 없어 requests가 ISO-8859-1로 디코딩 → "완성 아이템" 라벨이 깨져 페이지 파싱이 실패하고, 이후론 티어+골드 근사치로 대체하고 있었음 (1슬롯에 나중 갈 아이템·신발 위치가 사이트와 다랐던 이유)
  - **수정**: UTF-8 강제 디코딩으로 페이지 파서 부활 → 이제 챔피언 페이지의 **"완성 아이템" 6개를 사이트 순서 그대로** 표시 (아리: 루덴 → 마법사의 신발 → 폭풍 쇄도 → 그림자불꽃 → 라바돈 → 악의)
  - 우선순위: 사이트 페이지 순서 → 티어 근사치 → 패키지 스냅샷
  - **전체 173챔피언 오프라인 스냅샷도 패치 16.17 최신 사이트 순서로 재갱신** (캐시/오프라인일 때도 정확)
  - 증강 보드 버그 수정: 사이트 티어 버킷과 카탈로그 등급이 다른 증강이 다른 희귀도 칸에 섞여 표시되던 문제 (사이트 배치 우선)

### v1.6.108

- **🥾 아수라장 6슬롯 빌드 구매 순서 정렬 + 신발 누락 수정**
  - **신발 누락 수정** — 라이브 코어 5개가 6칸을 먼저 채워 신발이 빠지던 문제. 이제 라이브 티어 최선의 신발을 **3번째 슬롯**에 배치 (1·2코어 이후 첫 귀환 즈음 구매 관행)
  - **구매 순서 정렬** — 1슬롯에 나중에 갈 코어가 나오던 문제. 티어 순 → 골드 싼 순으로 정렬해 1슬롯이 첫 코어(최저가 스파이크)가 됨
  - 실측: 아리 `악의 → 루덴 → 마법사의 신발 → 폭풍 쇄도 → 리안드리 → 공허`, 아트록스/바루스/소라카/제드/탐 켄치 모두 신발 포함 + 챔피언 성향 정합
  - 코어가 5개 미만이면 태그 기반 폴백 코어로 6슬롯 채움

### v1.6.107

- **🚨 아수라장 브리핑 크래시 긴급 수정** — 라이브 경로에서 `build_url` 미정의로 ARAM 브리핑이 실패하던 버그 (어제 CI 정비 중 재구성하면서 유입, 즉시 수정 + 회귀 테스트 추가)
- **💎 증강 아이콘 깨짐 수정** — 제네릭 실버 아이콘(64×64)이 크기 검증(≥128px)에 걸려 placeholder로 표시되던 문제 (최소 32px로 완화, 표시 크기와 동일) — 실측 27/27 아이콘 렌더
- **🛡 안전성 코드 리뷰 9건 수정** (독립 리뷰 발견)
  - 밴픽 감지 dedup 시그니처 불일치 → **다른 탭을 보고 있어도 3초마다 아수라장 탭이 강제로 올라오던 탈취 루프 제거**
  - 타임라인 응답이 다른 섹션(주문·룬/스킬)의 "불러오는 중…" 자리를 파괴하던 교차 파괴 → 섹션별 플레이스홀더 분리
  - 증강 아이콘 다운로드 중 다른 챔피언 브리핑 실행 시 옛 결과로 화면을 덮는 경합 → generation 가드 추가
  - 부팅 스케줄(after) 예외 삼킴으로 자동 전적 로드·업데이트 확인이 유실될 수 있던 문제 복원
  - 복기 증강 아이콘 LCU 캐시가 실패(None)를 영구 기억하던 문제, 업데이트 설치 시 정상 종료 경로 우회, 워커 스레드 경합 등
- **🧹 라이브 증강 설명 정리 강화** — 게임데이터 플레이스홀더(??)·과도한 공백 제거, 160자 제한 / 라이브 JSON 응답 8MB 상한 (이상 응답 방어)

### v1.6.106

- **⬆ 앱 내 자동 업데이트 신뢰성 개선**
  - **설치 완료 후 앱 자동 재실행** — 기존엔 무음 설치에서 postinstall이 건너뛰어져 업데이트 후 앱이 안 떴음 (인스톨러 설정 수정)
  - **설치 시퀀스 경합 제거** — 앱을 완전히 종료한 뒤(프로세스가 exe 잠금을 내려놓은 뒤) 인스톨러가 실행되도록 순서 변경. 기존엔 종료 중 파일 잠금으로 설치가 실패할 수 있었음
  - **GitHub API 실패 시 우회 경로** — API 레이트리밋(403)이면 `releases/latest` 리디렉션에서 태그를 읽는 우회로로 자동 전환 + 1회 재시도 ("오프라인" 오탐 감소)

### v1.6.105

- **🛡 게임 중 오버레이 보호 + LLM 반복 루프 방어** — 게임 중 위젯에 "prowess prowess…"가 수천 줄 깔리던 문제 수정
  - **오버레이 보호**: 게임 중 증강 TOP3 오버레이가 표시되면 다른 요약(AI 코칭·브리핑)이 위젯을 덮어쓰지 않음 (탭에서는 정상 확인). 게임 종료 시 자동 해제
  - **반복 루프 정리기**: LLM이 같은 단어를 무한 반복하는 망가진 출력(예: prowess ×60)을 자동 접기 + 줄당 240자·전체 80줄 제한 — 위젯·복사본 모두 적용
  - AI 코칭은 게임 중 위젯 푸시를 생략 (탭 카드로 확인)

### v1.6.104

- **🎮 인게임 실시간 증강 추천 오버레이 (신규)**
  - 아수라장/ARAM 게임 시작이 감지되면 **미니 위젯에 챔피언 맞춤 증강 TOP3가 자동 표시** — 증강 선택 때 Alt+Tab 없이 화면 구석에서 확인
  - 위젯이 닫혀 있으면 자동으로 열고(항상 위 + 클릭 통과 지원), ARAM 자동 브리핑 요약이 오버레이를 덮어쓰면 최대 3회 재푸시로 복원
  - 설정 → 알림에 "게임 중 증강 추천 오버레이" 토글 추가 (기본 ON)
- **🏆 아수라장 티어표 (신규 페이지)**
  - blitz.gg 실시간 전체 173챔피언 티어(1~5)를 챔피언 아이콘 그리드로 표시 — 챔프 선택 전 참고·리롤 판단용
  - 좌측 사이드바 "티어표"에서 이동, 패치·데이터 시점 표기, 72h 캐시

### v1.6.103

- **⚔️ 아수라장 6슬롯 빌드도 실시간 전환 (blitz.gg)**
  - `aram_mayhem_champion` API의 **챔피언별 아이템 티어(89개)**를 실시간 조회해 코어 5슬롯을 현재 패치(16.17) 기준으로 구성 — 재료·구매 불가·칼바람 제외 아이템 필터링, 신발은 기존 태그 기준 유지
  - 라이브 아이템이 3개 미만이면 기존 패키지 스냅샷으로 폴백 (이전과 동일)
  - 아이템 ID는 API 원본 값을 직접 사용 — DDragon의 아레나 변형 아이템(223157 등) 한글명 중복으로 인한 역조회 오탐 회피
- **🎨 라이브 전용 신규 증강 아이콘 표시** — 패키지 카탈로그에 없는 신규 증강도 게임 클라이언트 에셋(증강 ID → 아이콘 경로)에서 아이콘을 가져와 표시 (클라이언트 실행 중, 이후 디스크 캐시)
- 증강·아이템 모두 이제 패치에 맞춰 자동 갱신 (72h 캐시)

### v1.6.102

- **💎 아수라장 "희귀도별 고정 TOP 3" → "챔피언 맞춤 TOP 3" 전면 개편 (blitz.gg 실시간)**
  - 챔피언을 누르면 blitz.gg 데이터 API(`aram_mayhem_champion`)에서 **현재 패치의 챔피언별 증강 티어 전체**(134개, 티어 1~5)를 실시간 조회해 실버·골드·프리즘별 맞춤 TOP 3 표시
  - 기존 패키지 스냅샷(패치 16.15, 8/15자)은 네트워크 실패 시 폴백으로만 사용 — 보드 하단에 출처·패치·데이터 시점 표기 (`blitz.gg 실시간 챔피언 티어 · 패치 16.17 · 데이터 2026-08-28`)
  - 제시 증강 판정(2번 보드)·메타 증강 추천(6순위)도 라이브 티어 순위로 개선 — 챔피언 페이지가 실제 쓰는 iesdev API를 그대로 사용, 72h 디스크 캐시
  - 증강 설명은 게임데이터 공식 한글 설명 사용 (마크업 태그 제거), 아이콘은 패키지 카탈로그 재사용
- **🐛 오래된 스냅샷 원인 규명** — blitz.gg 챔피언 페이지 HTML 구조 변경으로 기존 refresh 스크립트가 실패하고 있었음 → HTML 파싱 대신 데이터 API 직행으로 우회

### v1.6.101

- **🚨 설정창이 안 열리는 크래시 긴급 수정** — 설정을 열면 상태바에 `'._tkinter.tkapp' object has no attribute ...` 오류가 뜨며 창이 안 열리던 문제
  - 원인: 설정창이 `app._show_api_help`·`app._on_discord_review_toggle`을 참조하는데, 두 메서드는 예전 리팩터링(v1.6.8/v1.6.50)에서 me_tab 쪽으로 이동한 상태 — 다른 토글들은 `app.me_tab._on_*`으로 고쳐졌지만 이 둘은 누락
  - 수정: 디스코드 복기 토글은 `app.me_tab._on_discord_review_toggle`로 위임, API 도움말은 설정창 자체 메서드에서 `open_api_key_help`를 직접 호출 (탭 의존 제거)
  - 기존 마스킹 테스트 스텁이 앱에 직접 토글을 심어줘 원래 버그가 검증을 통과하던 문제도 함께 수정 + 설정창 오픈 회귀 테스트 추가

### v1.6.100

- **⚡ 기동 속도 2배 개선 (3.0초 → 1.4초)**
  - **패키징 onefile → onedir 전환** — 매 실행마다 임시 폴더에 압축을 풀고 백신이 재검사하던 구조 제거. 설치 폴더에 파일들이 바로 존재해 기동·백신 비용 대폭 감소
  - **탭 지연 빌드** — 기동 시 첫 화면(소환사의 협곡)만 그리고, 아수라장·내 전적·세션 리포트는 사이드바에서 처음 클릭할 때 빌드 (위젯 빌드 1.68초 → 0.84초, 재방문은 8ms)
  - 밴픽 감지·게임 종료 복기 등 자동 전환 경로에도 지연 빌드 가드 적용 — 어느 화면을 먼저 열어도 정상 동작

### v1.6.99

- **💎 아수라장 복기에 "획득 증강" 기록 추가** — 내 전적에서 아수라장 경기를 클릭하면 그 판에 먹은 증강을 아이콘·한글 이름으로 표시
  - LCU 경기 상세의 `playerAugment1~6`(숫자 ID)를 파싱해 CommunityDragon 카탈로그(7일 캐시)로 한글 이름·등급 매핑 — "판도라의 상자", "무리한 진입" 등
  - 증강 아이콘은 롤 클라이언트 에셋에서 받아 디스크 캐시 (클라이언트 꺼짐·조회 실패 시 이름만 표시, 전부 실패 시 섹션 숨김)
  - 등급별 색상: 프리즘=보라, 골드=골드, 실버=회색
  - Match-V5 `augments` 필드(아레나 등)도 함께 파싱 — 협곡 쪽 아레나 경기에도 확장 가능

### v1.6.98

- **🚨 v1.6.97 실행 크래시 긴급 수정** — exe 실행 시 `UnicodeDecodeError: 'cp949' codec can't decode` 로 앱이 안 켜지던 문제
  - 원인: v1.6.97 테마 JSON에 한글 폰트명(맑은 고딕)이 비-ASCII로 저장됐고, CustomTkinter가 파일을 기본 인코딩(cp949)으로 열어 디코드 실패
  - 수정: 테마 JSON 12종을 ASCII 이스케이프(`\uXXXX`)로 저장 — 어떤 시스템 인코딩에서도 안전, 폰트 패밀리는 동일하게 적용

### v1.6.97

- **🎨 UI 전면 개편 — 사이드바 셸 + 타이포그래피·아이콘 체계**
  - 상단 탭바를 **좌측 사이드바 내비게이션**으로 교체 (소환사의 협곡 / ARAM 아수라장 / 내 전적 / 세션 리포트 / 설정) — 선택 항목 골드 인디케이터 + 호버 하이라이트
  - 폰트 토큰 체계 도입: 숫자·영문 지표(KPI·승률)에 DIN 계열 **Bahnschrift 디스플레이 폰트** 적용, 한글 본문은 맑은 고딕 유지 — 전 스킨 통일
  - 버튼·내비의 장식 이모지를 **Segoe Fluent Icons 단색 아이콘**으로 교체해 스킨 색과 자동 조화 (미탑재 환경은 한글 텍스트로 자동 폴백)
  - 섹션 제목의 선행 이모지 자동 정리 — 타이포 위계로만 강조
- **📌 미니 위젯 → 진짜 오버레이**
  - **투명도 슬라이더**(50~100%)와 **클릭 통과(클릭스루)** 토글 추가 — 게임 플레이를 가리지 않음
  - 클릭 통과 중에도 전역 핫키 Ctrl+Shift+W로 해제 가능
- **📊 오늘의 세션 리포트 (신규 페이지)**
  - 오늘 하루 경기를 Riot API에서 직접 조회해 게임 수·승패·승률·평균 KDA를 큰 숫자 KPI 카드로 요약
  - 연승/연패 감지(3연패 시 휴식 가이드), 주력 챔피언 집계, "다시 보면 좋은 경기" → 클릭 시 복기 바로 열기
  - 요약이 미니 위젯에도 자동 푸시되어 인게임에서 확인 가능 (결정론 계산 — LLM 불사용)
- **🐛 낡은 테스트 스텁 갱신** — v1.6.96 적 챔피언 필드(enemies_by_role) 미반영으로 실패하던 GUI 스레딩 테스트 수정

### v1.6.93

- **🐛 전체 탭 아수라장 안 보임 수정** — "전체" 탭에서 아수라장 경기가 안 보이던 문제 해결
  - Riot API 전적과 LCU 로컬 아수라장 전적을 합친 뒤 시간 정렬 없이 표시해 아수라장이 목록 맨 뒤로 밀려 안 보였음
  - game_end_timestamp 기준 내림차순 정렬 추가 — 전체 탭도 최근 경기 순으로 정렬되어 아수라장이 정상 표시
- **⚡ 반응 속도 추가 개선** — 앱 부팅·사용 중 답답함 호소 해소
  - 밴픽 워처(AlwaysOnChampSelect)가 게임 없을 때도 3초 고정 폴링 → 밴픽 감지 시 3초, 미감지 시 1.5배 백오프(상한 6초)로 CPU 부하 감소
 - 밴픽 폴링이 매번 LCUClient를 새로 생성해 lockfile 탐색+연결 확인을 반복하던 병목 제거 — 클라이언트 캐싱 후 재사용, 타임아웃 1.5s로 단축

### v1.6.94

- **🐛 결과 필터 "전체"에서 아수라장 안 보임 수정** — 승/패는 보이는데 "전체"만 안 보이던 버그
  - 정렬된 경기를 "필터가 걸렸을 때만" 재집계해서, 필터 없는 "전체"는 정렬 전 원본 순서(아수라장이 맨 뒤)로 렌더링됨
  - 항상 정렬·필터 결과로 재집계하도록 수정 — "전체"도 최근 경기 순으로 아수라장 정상 표시

### v1.6.96

- **🎯 아수라장 적 챔피언 입력 + 적응형 빌드** — 수동/밴픽 브리핑에서도 조합 분석 켜짐
  - ARAM 탭에 적 챔피언 5칸 입력 추가 (선택 사항, 비워도 기존처럼 브리핑)
  - 적 입력 시 조합 위협·시너지 분석 + 아이템 4~6슬롯 자동 분기 (적 탱커 2명→관통, 힐러 2명→치감 등)
  - 인게임 자동검색 시 적 챔피언 칸 자동 채우기

### v1.6.95

- **🤖 AI 코칭 모델 추가 — Mimo-V2.5** — 설정에서 코칭 모델 선택 가능
  - deepseek-v4-flash 가격 인상에 대응해 opencode-go 게이트웨이에 mimo-v2.5 모델 추가
  - 설정 → AI 코칭 → 모델 드롭다운에서 선택 가능 (기본값은 기존 deepseek-v4-flash 유지)

### v1.6.89

- **📊 복기 카드 스킬 순서 + 소환사 주문·룬** — 매치 상세(복기)에 이미 수집했지만 안 보이던 정보 표시
  - 타임라인에서 스킬 업 순서(Q/W/E/R)를 파싱해 "스킬 순서" 섹션으로 표시
  - 소환사 주문 아이콘 2개 + 주 룬 아이콘을 "소환사 주문 · 룬" 섹션으로 표시 (DataDragon 아이콘)
  - "내 지표"에 DPM(분당 데미지) 추가 — 딜·골드·킬관여·딜지분 옆에

### v1.6.92

- **🐛 아수라장 전적 로드 회귀 수정** — v1.6.91에서 아수라장 전적이 안 불러와지던 버그 긴급 수정
  - v1.6.91에서 추가한 LCU 큐 필터(queue=2400)가 LCU 미지원 파라미터라 HTTP 400 발생
  - 큐 필터 제거, 원래 로컬 전적 조회 방식(build_local_form)으로 복구 — LCU 캐시 최근 게임 그대로 수집
  - 완화된 보충 조건(아수라장 < 10판) 유지 — 아수라장 위주 플레이어 전적 정상 표시
- **⚡ 반응 속도 개선** — 게임 없을 때 백그라운드 폴링 부하 저감
  - Live Client Data API 폴링 타임아웃 1.5s → 0.4s 단축 (게임 미실행 시 빠른 실패)
  - 게임 없으면 점진적 백오프(3s→5s→8s→12s, 상한 15s)로 CPU/네트워크 부하 감소
  - 게임 감지 시 즉시 기본 간격(3s) 복귀 — 감지 지연 없음

### v1.6.91

- **🏆 아수라장 전적 보충 강화** — Riot API 키가 있을 때 아수라장(큐 2400) 전적이 누락되던 문제 개선
  - 아수라장은 Match-V5에 인덱싱되지 않아 LCU 로컬에서 보충하는데, 기존엔 "아수라장이 0개일 때만" 동작해 1~2개만 보이는 계정이 많았음
  - 보충 조건을 "아수라장이 10판 미만일 때"로 완화하고, LCU 매치 히스토리를 큐 필터로 직접 조회해 칼바람 게임에 밀려 누락되던 아수라장 게임까지 수집
  - match_id 기준 중복 제거 후 기존 전적과 병합 — ARAM 아수라장 위주 플레이어의 "내 전적"·복기가 훨씬 풍성해짐

### v1.6.90

- **🖼️ 복기 카드 소환사 주문·룬 아이콘 표시 수정** — 첫 실행(콜드 캐시)에서 아이콘이 안 보이던 버그 수정
  - 메인 스레드 동기 렌더링이 원인 — 아이콘 캐시가 비어 있으면 다운로드가 안 일어남
  - 타임라인·킬맵과 같은 백그라운드 패턴으로 변경: 백그라운드에서 DataDragon 아이콘 다운로드 → 캐시 → 메인 스레드 콜백으로 표시
  - DataDragon(공식 CDN) 소스 유지 — 점멸·점화·회복·탈진 등 소환사 주문과 감전·정복자 등 주 룬 아이콘이 정상 표시

### v1.6.88

- **🎮 게임 시작 자동 브리핑** — Live Client Data API 기반 게임 입장 시 자동 브리핑
  - 3초 폴링으로 게임 시작 감지, 로딩 화면에서도 챔피언 식별 (summonerName 매칭)
  - ARAM/아수라장 모든 모드 지원, 밴픽 브리핑과 dedup로 중복 방지
  - 게임 종료 후 새 게임 입장 시 자동으로 새 챔피언 브리핑 전환
- **🔒 자동업데이트 보안 강화** — 다운로드 후 SHA256 재검증(TOCTOU 방지) + 리다이렉트 호스트 허용목록
  - 다운로드 시점 캐시된 해시 무시, 항상 GitHub에서 재요청 → 변조된 캐시로 교체 공격 차단
  - `github.com` → `objects.githubusercontent.com` 등 허용 호스트만 리다이렉션 허용 (악의적 호스트 우회 차단)
- **🔒 전체 코드 리뷰 발견사항 수정** — 보안·안정성·품질 12개 파일 수정
  - 폴러 데몬 스레드화(UI 블로킹 해결), PUUID 로그 마스킹, .env 권한 강화, 임시파일 예측 가능 경로 제거
  - winrate 색상 헬퍼 DRY화, 적 시너지 "까다로운 상대" 섹션 추가, 차트 축 라벨·docstring 정정

### v1.6.87

- **🏆 포지션별 승률 카드** — 내 전적 요약에서 포지션(TOP/JGL/MID/ADC/SUP)별 승률·KDA·평균 데스 표시
  - SR 큐(솔로랭크/자유랭크/일반)만 집계 — ARAM은 역할이 무의미
  - 승률 55%+ 녹색, 45% 미만 적색으로 직관적 색상
- **🤝 챔피언 시너지 통계** — 같이 뛴 아군 챔피언별 승률 — "이 챔피언과 하면 승률 N%"
  - ally_team 데이터에서 is_me 제외한 아군 챔피언 집계 (최소 2판)
- **📈 전적 성장 차트** — 최근 20판 KDA + CS/분 추이를 선 차트로 시각화
  - KDA(녹색) + CS/분(금색) 라인, 승패 배경 칩 — 외부 라이브러리 없이 Canvas로 구현
- **🔮 실시간 승패 예측** — 게임 중 Live Client Data(포트 2999) 킬 차이로 실시간 승률 갱신
  - 게임 시작 90초 후 30초 간격 폴링, 상태바에 "실시간 승률 N% (X분) · 킬 ±N · 우세/불리/팽팽" 표시
  - 초기 예측(조합+폼)을 베이스로 킬 차이 ±2%/킬 보정 (최대 ±25%)
- **⚠ ARAM 증강 데이터 신선도 배너** — 카탈로그 데이터 경과일 표시 (정직한 출처 원칙)
  - 7일+ ℹ 정보성 안내, 14일+ ⚠ 경고 — 최신 패치와 다를 수 있음 명시

### v1.6.86

- **🔍 전적 목록 챔피언 검색 + 승/패 필터** — 내 전적 탭에서 챔피언명으로 검색하고 승/패만 따로 보기
  - 검색창에 챔피언 이름 입력 시 300ms 디바운스로 자동 필터링 (한글/영문 모두)
  - 전체/승/패 칩으로 결과 필터 — 큐 필터와 조합 적용
  - 필터링 시 "(전체 N판 중)" 표시로 필터 상태 안내
- **🔄 수동 업데이트 확인** — 헤더 🔄 버튼을 항상 클릭 가능하게 변경
  - 클릭 시 GitHub 최신 릴리스 확인 → 새 버전이면 설치, 최신이면 "최신 버전입니다 (v{cur}) · 오늘 날짜 기준" 표시
- **🐛 스킨 전환 깨짐 수정** — 밝은 스킨(sky/light) 전환 시 루트 창 배경이 구 테마로 고정되던 문제 수정
  - `set_default_color_theme` → `set_appearance_mode` 순서 보장 + 루트 배경 강제 갱신
- **🐛 설정 창 다중 모니터 위치 수정** — 보조 모니터에서 설정 창이 주 모니터로 튕기던 문제 수정
  - 부모 창 중앙 기반 배치 (화면 크기 클램프 제거)
- **🐛 Riot API 전적 puuid 불일치 수정** — 계정 이전/리전 마이그레이션 시 puuid 불일치로 전적이 안 불러와지던 문제 수정
  - Riot ID(game_name/tag_line) fallback 추가
- **🐛 아수라장 전적 LCU 보충** — Riot API가 아수라장 매치를 인덱싱하지 않는 제약 보완
  - Riot API 전적에 아수라장 매치가 없으면 LCU 로컬에서 보충 (하이브리드 병합)
- **🎨 ARAM 탭 UI 축소** — 백버튼·헤더·섹션3·AI 코칭 본문 크기 조정으로 콘텐츠 표시량 증가


### v1.6.85

- **🐛 첫 실행 설정 창 하단 버튼 짤림 수정** — setup_dialog에서 안내 내용이 길어 '저장 후 시작' 버튼이 화면 밖으로 나가 드래그해야 보이던 문제
  - 원인: 전체 내용을 `pack`만으로 쌓아 창 높이 초과 → 스크롤 없이 버튼 짤림
  - 해결: 본문을 `CTkScrollableFrame`으로 감싸 스크롤 가능하게, 버튼은 창 하단에 고정 → 항상 보임

### v1.6.84

- **🐛 인스톨러 설치 차단 버그 수정** — 친구 PC에서 설치 시 `dist\롤실전코치.exe 파일을 찾을 수 없습니다` 오류로 설치가 막히던 문제 해결
  - 원인: `InitializeSetup`이 빌드 환경 경로(`dist\`)를 런타임에 검사 → 사용자 PC엔 없어서 설치 중단
  - 해결: 런타임 빌드 체크 제거 (exe는 컴파일 타임에 인스톨러에 포함됨)
- **🐛 ARAM 5슬롯 아이콘 깨짐 수정** — 적응형 빌드 5슬롯(치감) 아이템명 오류
  - 원인: `치사의 검`은 DataDragon KR 명칭이 아님 → ID 매핑 실패 → 플레이스홀더 아이콘
  - 해결: 정식 명칭 `필멸자의 운명` (Mortal Reminder)으로 수정 → 정상 아이콘 표시

### v1.6.83

- **🎨 네온 아레나 스킨 추가** — 보라 메인 액센트 + 시안 보조 액센트의 다크 스킨
  - 설정에서 "네온 아레나 — 보라·시안 네온" 선택 (재시작 없이 라이브 전환)
  - 기존 "네온"(시안 메인)과 보라 메인으로 명확히 구분
- **🔀 ARAM 브리핑 순서 변경** — 아이템 빌드를 메타 증강 추천 앞으로 이동
  - 2번: 6슬롯 완성 빌드 (아이템) ← 먼저 표시
  - 3번: 메타 증강 추천 ← 뒤로 이동

### v1.6.82

- **🛡️ 안정성 하드닝** — 전체 앱 안정성 테스트 기반 예외 처리 강화
  - **위젯 geometry 복원 안전장치**: `after(500)` 콜백에서 위젯이 destroy된 후 `self._widget`이 None일 때 발생할 수 있는 `AttributeError` 방지 — `winfo_exists()` 체크 추가
  - **AI 코칭 텍스트박스 폴백**: `CTkTextbox._textbox` 비공개 속성 접근에 `getattr` 폴백 추가 — CTk 버전이 내부 구조를 변경해도 태그 없이 일반 텍스트로 폴백하여 크래시 방지
  - **위젯 폰트 크기 로드 예외 처리**: `widget_font_size`가 잘못된 타입(문자열 등)일 때 `ValueError` 방지 — 기본값 11pt로 폴백
  - **위젯 wraplength 동적 갱신**: 폰트 크기 변경 시 `wraplength`도 위젯 폭에 맞춰 갱신 — 폰트를 키워도 텍스트가 위젯 밖으로 넘어가지 않음

### v1.6.81

- **✨ 위젯 폰트 크기 조절 기능** — 미니 위젯 헤더에 −/＋ 버튼 추가, 코칭 내용 폰트 크기를 사용자가 직접 조절 (9~20pt)
  - 크기 설정은 `ui.json`(`widget_font_size`)에 저장되어 다음 실행 시 복원
  - 실시간으로 본문 라벨 폰트 일괄 적용

### v1.6.80

- **🐛 AI 코칭 카드 전체 스크롤 가능 + 가독성 개선** — 핵심 포인트가 화면에 안 보이던 문제 최종 수정
  - 핵심 포인트 박스(별도 Frame) + 본문 박스(Textbox) 2단 분리 → **단일 스크롤 텍스트박스로 통합**. 한 번에 스크롤하며 전체 내용 확인 가능
  - 텍스트 태그로 가독성 향상: 섹션 헤더(골드 굵게), 핵심 포인트(골드소프트), 본문(밝은 텍스트), 구분선
  - 핵심 포인트 상단 + 구분선 + 상세 코칭 하단 구조
- **✨ 아수라장 브리핑 AI 코칭 개편** — 아이템 제거, 인게임 조합 기반 실전 팁 강화
  - **아이템 섹션 제거**: 아이템 빌드는 화면에 따로 표시되므로 AI 코칭에서 제외. 이전 1~6코어 설명 6줄 삭제
  - **조합 분석 강화**: 양 팀 조합 기반 강점/위협 분석 + 승리 조건 + 증강 선택 + 초반/한타 행동 + 주의할 상대
  - **SR 전용 금지 가드 추가**: 정글 캠프·오브젝트·라인 관리 언급 금지
  - **간결 구조화**: 쓸데없는 일반론 배제, 이 조합에 맞는 구체적 실전 팁만 요구
  - max_tokens 3000→2000 (아이템 섹션 제거로 출력 단축)

### v1.6.79

- **🐛 위젯 멀티모니터 위치 복원 수정** — 위젯이 메인 모니터로만 뜨던 근본 원인 수정
  - **근본 원인**: 위젯 geometry가 저장되지 않았음 — 위젯을 켜두지 않고 종료하거나 위젯만 닫으면 위치 저장이 안 됨
  - **실시간 저장**: 위젯 이동/리사이즈 시 `<Configure>` 이벤트로 300ms 디바운스 후 자동 저장
  - **닫을 때 저장**: 위젯을 닫아도 최종 위치 저장
  - **복원 안전장치**: `_geo_ready` 플래그로 초기 맵핑의 기본 위치가 저장된 위치를 덮어쓰지 않게 차단
  - **깜빡임 방지**: 위젯 생성 시 화면 밖에서 초기화 후 복원 위치로 이동

### v1.6.78

- **🐛 AI 코칭 핵심 포인트 박스 가림 수정** — 본문 텍스트박스 `expand=True`가 핵심 포인트 박스를 뷰포트 밖으로 밀어버려 보이지 않던 문제 수정. `expand=False`로 본문이 고정 높이만 차지하고 핵심 포인트가 바로 아래 표시됨
- **✨ 멀티모니터 창 위치 복원** — 앱·위젯이 메인 모니터로만 뜨던 문제 수정
  - `clamp_window_geometry`가 가상 화면(virtual screen)을 인식하도록 개선 — Windows `GetSystemMetrics(SM_XVIRTUALSCREEN 등)`로 전체 모니터 영역 획득
  - 창이 가상 화면과 겹치면 그대로 유지, 완전히 벗어나면 메인 모니터로 fallback
  - 보조 모니터에 놓고 끄면 다음 실행 시 보조 모니터에 그대로 복원 (앱·위젯 모두)

### v1.6.77

- **🎨 AI 코칭 카드 가시성·품질 개선** — 경기 복기 AI 코칭 텍스트 우측 잘림 수정 및 디자인 개선
  - **텍스트 잘림 수정**: `wraplength=940` 하드코딩 → `<Configure>` 바인딩 기반 동적 래핑(`_wrap_dynamic`). 패널 폭에 관계없이 텍스트가 잘리지 않음
  - **핵심 포인트 분리**: 어둡고 잘리던 단일 줄(`핵심 · a | b | c`) → 골드 테두리 별도 박스 + 각 핵심을 개별 줄로 표시, `TEXT_BRIGHT` 적용으로 대비 향상
  - **섹션 헤더 강화**: 이모지 아이콘 추가(📋·⭐), 여백 정돈
  - **본문 텍스트박스**: 높이 상한 640→560px, 패딩 12→14px로 여유 확보

### v1.6.76

- **🐛 경기 복기 AI 코칭 ARAM 환각 수정** — ARAM(칼바람·아수라장) 경기 복기에서 AI가 정글 캠프·늑대·두꺼비·오브젝트·CS 150 같은 소환사의 협곡 전용 조언을 환각하던 문제 수정. `coach_review` 프롬프트에 게임 모드를 주입하고, ARAM이면 SR 전용 개념 언급 금지 지시 추가

### v1.6.75

- **🐛 아수라장 전용 계정 전적 0판 버그 수정** — Match-V5에 인덱싱 안 되는 아수라장(큐 2400) 매치만 있는 계정에서 Riot API가 빈 전적을 반환하면 자동으로 LCU 로컬 전적(롤 클라이언트 전적)으로 폴백. 이전에는 빈 form이 예외 없이 반환돼 0판으로 표시되고 로컬로 넘어가지 않았음

### v1.6.74

- **🧹 ARAM 제시 증강 입력·판정 제거** — 사실상 동작하지 않던 기능 정리
  - LCU 자동 입력: ARAM 밴픽에 증augment가 없어 동작 안 함 (이전부터 미동작)
  - 수동 입력: 레벨 3·7·11·15에 뜬 증augment 이름을 일일이 타이핑해야 했음
  - 입력 칸 + 증augment 목록 선택 버튼 + 판정/검증 로직 전부 제거
- **✨ 메타 증강 추천으로 전환** — 챔피언만 입력하면 blitz 메타 순위 TOP 추천 + 시너지 표시
  - 더 이상 제시 증augment를 입력할 필요 없이 챔피언만으로 증augment 가이드 제공
  - LCU 챔피언 자동 입력은 유지 (밴픽에서 챔피언만 자동 판정)

### v1.6.73

- **🧹 증강 화면 인식(OCR) 제거** — 인식률이 낮고 게임 프레임을 떨어뜨려 제거
  - 레벨 3·7·11·15 자동 캡처, `Ctrl+Shift+A` 핫키, 증강 창 레벨 알림 전부 제거
  - 증강 추천은 **그대로 작동** — 밴픽 중 LCU 자동 입력, 또는 증강 이름 직접 입력
  - 게임 중 화면 캡처가 사라져 프레임 저하 없이 깔끔하게

### v1.6.72

- **🎨 ARAM 탭 UI 리디자인 (blitz.gg 스타일)** — 3단계 전면 개편
  - **1단계**: 카드 곡선 12→16px, 섹션 바 4→5px 굵게, 타이포 FT(20)/FS(15 bold) 위계 정리, 챔피언 초상화 52→72px, 증강/아이템 아이콘 34→48px·38→52px 대형화
  - **2단계**: 빈 결과 영역 → **클릭 가능 챔피언 그리드** (24개 인기 챔피언, 6열×4행, 호버 골드 테두리), 타일 클릭 시 즉시 브리핑 실행 · 폼 경량화(버튼 38→32px, 패딩 축소)로 결과 영역 강조
  - **3단계**: 버튼 위계 정리 (핵심 CTA만 골드, 보조는 SECONDARY/TERTIARY), 섹션 제목 바 대비 강화 (GOLD_SOFT→TEXT_BRIGHT), 결과 카드 호버 효과 (테두리→골드, 배경→밝게), 핵심 버튼 타이포 11→13pt 가독성 향상
  - 아이콘 폴백 버그 수정: 72px/52px 캐시 없을 때 기존 캐시에서 리사이즈 (초상화/아이템 안 나오는 문제 해결)

### v1.6.71

- **🤖 증강 자동 인식 강화** — 레벨 3·7·11·15 도달 시 더 이상 Ctrl+Shift+A를 안 눌러도 됩니다
  - 창이 뜨면 자동으로 화면을 캡처해 증강 3장을 읽고 등급 판정까지 한 번에
  - 인식 실패 시 1.8초 간격으로 최대 3회 백오프 재시도 (증강 창이 열려 있는 동안)
  - 성공한 레벨은 기억해 중복 캡처 방지 · 첫 캡처 타이밍을 0.5초→0.9초로 늘려 창 렌더링 대기
  - 3회 전부 실패시에만 수동(Ctrl+Shift+A) 안내 · 토스트도 “자동으로 읽는 중”으로 정리
  - 주의: 롤이 **전체화면 모드**면 화면이 검게 캡처되어 자동·수동 모두 안 됩니다 — 비디오 → 테두리 없는 창 모드 필수

### v1.6.70

- **🎲 리롤 결정 어드바이저** — 아수라장 밴픽에서 내 챔프가 잡히는 순간 Blitz 챔프별 티어로 리롤 판단
  - 하위(B) 티어면 “리롤로 더 나은 챔프를 노려 보세요”, 상위(S)면 “리롤 보류 추천”
  - A등급은 모호하므로 말하지 않는다 (정찰 칩과 같은 침묵 원칙) · 데이터 부족 시 표시 안 함
- **✦ 증강 시너지 가이드** — 추천·회피 증강의 시너지 근거를 한 줄로 표시
  - 챔프 성향(메이지/탱커/암살자…)과 증강 archetype 태그 매칭으로 “X 시너지”/“X 주의” 안내
- **🔧 적 조합 적응형 빌드** — 인게임 자동검색 시 적 조합 태그로 4~6슬롯 분기
  - 적 탱커 2+ → 4코어 관통(메이지: 공허의 지팡이 / 물리: 도미닉 경의 인사)
  - 적 서폿 2+ → 5코어 치사의 검(치감) · 적 마법 3+ → 6코어 MR · 적 암살 2+ → 6코어 수호천사
  - 1~3코어는 고정, 분기 안내 줄과 함께 표시 · 적 조합 없으면 기존 빌드 그대로
- **🎯 증강 창 레벨 타이밍 알림** — 레벨 3·7·11·15 도달 시 “첫/두 번째/세 번째/네 번째 증강 창” 토스트
  - 잠깐 멈춰 화면을 읽으라고 안내 (Ctrl+Shift+A) · 기존 OCR 자동 캡처는 유지

### v1.6.69

- **복기 팀 줄** — `미상` 대신 Riot ID (`닉#태그`). 협곡은 `탑  닉#태그  아리`처럼 라인+아이디

### v1.6.68

- **내 전적 로드** — 클라이언트가 로비에만 있어도 lockfile 오류로 막히던 문제
  - 예전처럼 API 키가 있으면 Riot API로 전적을 불러옵니다
  - API가 실패해도 진짜 원인을 가리지 않고, 클라이언트 대체는 로비 대기 중에도 됩니다

### v1.6.67

- **🤖 AI 프로바이더 선택** — 설정에서 골라 씁니다
  - `opencode-go` — 기존과 동일. 키 비우면 OpenCode CLI 자동 감지
  - `Gemini` — 구글 키. 결제 연결하지 않으면 한도까지 무료
  - `Groq` — 카드 없이 무료. 한도 넘으면 거절
  - `OpenRouter` — **브라우저로 연결(OAuth)** 또는 키. `:free` 모델은 크레딧 없이 하루 약 50회
- 프로바이더마다 키를 따로 저장해서 바꿔도 이전 키가 남습니다
- 연결 확인은 지금 고른 프로바이더를 두드립니다

### v1.6.66

- **🤖 AI 코칭 설정** — 프로바이더는 opencode-go 로 고정 표시, 입력칸은 `API 키`, 연결 확인 버튼 추가

### v1.6.65

- **📬 CI 실패 메일** — ruff/mypy가 깨져 GitHub이 계속 Failed 메일을 보내던 문제 수정
- **🖼 깨진 증강 아이콘** — 없는 아이콘을 받은 뒤 판정을 한 번 다시 그림
- **일반 S vs 이 챔프 S** — 마도사의 주스 상자처럼 전체 메타 S는 `일반 S`, 지금 챔프 전용 순위는 `이 챔프 S`

### v1.6.64

- **🃏 제시 3장 전부 표시** — 존야 업그레이드처럼 긴 옆 카드에 가려 빠지던 문제
  - 칸이 부족한 게 아님. 긴 제목 박스에 덮여 같은 열로 묶이면 하나를 버리고 있었음
  - 제목을 못 읽으면 카드 설명으로 찾고, 추천/비추가 아닌 제시 카드도 판정 칸에 표시

### v1.6.63

- **🃏 빠진 1장 복구** — 같은 칸만 안 읽고 “글자를 못 읽었다”고 뜨던 문제
  - 화면 반전 OCR을 먼저 쓰지 않음 (글자가 지워지던 원인)
  - 창 좌표를 DPI에 맞게 잡아 한쪽 카드가 잘리지 않게 함
  - 깨진 글자도 카탈로그에 맞추고, 2장만 읽히면 그렇게 안내

### v1.6.62

- **🃏 제시 3장 모두 읽기** — 한 칸만 빠지던 문제
  - OCR이 두 장을 한 줄로 붙여도 이름을 전부 살림
  - 안 읽힌 칸은 좌·중·우를 잘라 한 번 더 읽음

### v1.6.61

- **🎯 제시 3장만 읽기** — 실버 TOP 3 같은 앱 추천 글을 맵 카드로 착각하던 문제
  - 롤 창만 찍고, 겹친 위젯/오버레이는 찍기 전에 숨김
  - 가운데 좌·중·우 3열에서 이름이 2장 이상일 때만 인식

### v1.6.60

- **👁 증강 읽기 인게임 피드백** — `Ctrl+Shift+A` 눌러도 앱이 조용하던 문제
  - 게임 중에도 결과/실패를 바로 띄움 (검은 화면이면 테두리 없는 창 모드 안내)
  - OCR용 PowerShell 콘솔이 깜빡이지 않게 숨김
  - 롤 위에 작은 안내 창을 띄움 (전체화면 전용은 게임이 가릴 수 있음)

### v1.6.59

- **👁 아수라장 제시 증강 화면 읽기** — 맵에서 3장이 뜨면 붙여넣지 않고 판정
  - 레벨 3·7·11·15가 되면 화면 중앙을 Windows 한글 OCR로 읽어 카탈로그와 맞춤
  - 게임 포커스 중에도 `Ctrl+Shift+A` 로 다시 읽기
  - 밴픽 LCU 404를 “클라이언트 없음”으로 오인하던 안내 수정 — 증강은 밴픽이 아니라 맵에서 뜸


### v1.6.58

- **⚡ 밴픽 중 아수라장 자동 브리핑** — 앱만 켜 두면 LCU 밴픽을 상시 추적
  - 내 챔프가 잡히는 순간 ARAM 탭으로 전환하고 TOP 3·6슬롯을 바로 표시 (밴픽 버튼 불필요)
  - 제시 증강이 뜨거나 리롤되면 판정만 갱신 · 협곡 밴픽은 건드리지 않음
  - API 키·전적 로드 없이 동작 (롤 클라이언트만 있으면 됨)


### v1.6.57

- **🎯 아수라장 시작 시 자동 브리핑** — 큐 2400이 감지되면 ARAM 탭으로 전환하고 내 챔프 + 희귀도 TOP 3 + 6슬롯을 바로 표시 (자동 검색 버튼 불필요)
  - 시작 토스트가 칼바람과 아수라장을 구분 (`아수라장 · 내 챔피언 X`)
  - 승패 예측 폼은 아수라장 전적 우선 (표본 부족 시 칼바람 → 전체)
- **🧱 LiveSession 분리** — 종료 워처·리메이크 게이트·모드 판정을 tk 없는 `gui/live_session.py`로 이동 (1단계 구조 정리)


### v1.6.56

- **🛠 라이브 정산·정찰 버그 수정** — 리메이크/조기항복이 본판 예측·팀운·디스코드를 잡아먹던 경로를 닫음
  - 같은 로스터 예측은 가장 최근 것만 소비하고 오래된 잔여는 삭제
  - 5분 미만·조기항복 경기는 성적표/팀운/복기 카드를 보내지 않음
  - 새 게임 ID가 보이면 종료 워처를 교체해 본판 종료 감지가 빠지지 않음
  - 이기고 활약이 적으면 "네가 캐리했습니다"로 뒤집히지 않음
  - 정찰 이름을 Spectator-V5 `riotId`로 표시, 상세 조회도 페이싱·429 시 중단
  - 디스코드 오류 토스트에 웹훅 토큰이 나오지 않음
  - 로컬 전적 본인 판정은 puuid 우선, 이름은 `#태그`를 빼고 비교


### v1.6.55

- **🔍 10인 자동 정찰 + 리드 칩** — 게임 시작 시 양팀 9명 최근 전적을 자동 스캔해 브리핑 (loldosa식 정찰)
  - 리드 칩: "오늘 N판째(W승 L패)" · "방금 패배 후 재큐 — 빡큐" · "원챔 X (5판 중 4판)" · "폼 핫/콜드" — 전부 결정적 계산
  - 표본 3판 미만이면 침묵 (말하지 않는 원칙) · 시작 시 토스트 + 디스코드 정찰 카드
  - Riot rate limit 안전: 플레이어당 리스트 1회 + per-puuid 30분 TTL 캐시 + 순차 pacing + 실패 스킵


### v1.6.54

- **🔒 보안·구조 정리** — 전체 코드 리뷰 반영
  - LLM 요청 프록시 격리 강화: `secure_session(trust_env=False)` 전환 — 환경 프록시/CA로 키가 흐르는 경로 차단 (요청별 프록시 무력화와 이중 방어)
  - 설정창 디스코드 웹훅 URL 마스킹(show="•") — API 키와 동일 정책
  - 디스코드 전송 5개 래퍼 → 공통 `_post_discord_card` 헬퍼 통합 (제목·PNG 전부 지연 호출 — 웹훅 미설정 시 부수 효과 없음)
  - me_tab.py 1,883줄 → 매치 상세 뷰를 `me_detail_mixin.py`(671줄)로 분리
  - ruff format 전면 적용 (76개 파일 포맷 완료)


### v1.6.53

- **⚖️ 이 판 누구 탓 % — 팀운 정산** — 게임이 끝나면 책임을 나·팀·상대 3분해로 정량화 (예: 나 13% · 팀 68% · 상대 19%)
  - 점수 = KDA(어시 0.7 가중) + 팀 내 데미지 비중 — 이해 가능한 투명 공식, 표본(팀원 3명 미만)이 없으면 말하지 않음
  - 판정 한마디: "네 탓 아님" / "내 탓이 큽니다" / "팀 전체가 같이 무너졌습니다" / "네가 캐리했습니다"
  - 종료 시 토스트 + 디스코드 팀운 정산 카드 (나/팀/상대 3색 바)


### v1.6.52

- **🔮 승패 예측 → 🧾 성적표 정산 루프** — 게임 시작(로딩) 시 양팀 조합 + 내 최근 폼으로 승률 예측, 끝나면 예측 vs 결과 성적표 카드
  - 예측: 앞라인 차이·힐/유틸·풀 AD/AP 패널티 등 조합 균형 ±16% + 내 폼(최근 5판+ 표본) ±12%, 20~80% 클램프 — 전부 코드 계산 (LLM 없음, 표본 부족 항목은 조용히 제외)
  - 시작 시 토스트 "🔮 승률 예측 N%" + 디스코드 예측 카드, 종료 시 "🧾 예측 성적표 — 적중/빗나감" + 이 판 한마디
  - 예측은 로스터 시그니처로 소비 — 닷지·리메이크는 잘못 매칭되지 않고 6시간 지난 미소비 예측은 자동 정리


### v1.6.51

- **⚡ 증강 실시간 판정** — 아수라장 밴픽에서 증강 3개가 뜨는 순간 LCU가 감지해 **즉시 판정** (토스트 "🎯 증강 판정 — X 선택" + 디스코드 판정 카드 자동 전송)
  - 리롤하면 새 목록으로 자동 갱신 (이전 자동 입력만 교체, 수동 수정은 보존)
  - 판정은 카탈로그/Blitz 티어 기준 — 제시된 증강 안에서만 1~3순위 + 회피 권장 카드 렌더
  - 화면 캡처·OCR 없음 — 롤 클라이언트 로컬 API(LCU)의 밴픽 세션에서 직접 읽음


### v1.6.50

- **📮 디스코드 복기 카드 자동 전송** — 게임이 끝나면 킬 지도·붕괴 스냅샷·복기 요약을 담은 카드가 디스코드 채널로 자동 도착 (loldosa식 UX)
  - 설정창에서 웹훅 URL 붙여넣기·테스트 전송·자동 전송 on/off 전부 GUI로 (명령줄 불필요)
  - CLI `lol-coach discord set/url/test/clear` 도 제공 (웹훅 토큰 마스킹·https+디스코드 호스트 검증·8MB 한도·429/5xx 재시도)
  - 카드에는 게임 모드(칼바람·아수라장·협곡)·KDA·킬 지도·붕괴 스냅샷·이 판 핵심/잘한 점/다음 판 행동/교훈 포함 — 아수라장(queueId 2400)도 동일 지원


### v1.6.49

- 🔌 **키 없는 로컬 전적 모드** — Riot API 키가 없거나 만료돼도 롤 클라이언트(LCU) 로컬 API로 내 전적·복기·트렌드가 그대로 동작 (타임라인 엔드포인트가 있으면 킬 지도까지)
- 🔑 **개인 키 안내** — 개발 키 만료(403) 시 Personal 앱 등록 안내 다이얼로그 + 도움말 개선 (개인 키는 24시간 만료 없음)


### v1.6.48

- **🖼 아수라장 실제 아이템 아이콘 복구** — 6슬롯 빌드가 한국어 이름을 다시 추측하지 않고 Blitz 원본 아이템 ID를 유지해 아수라장 전용 아이템까지 실제 아이콘으로 표시
- **💾 아이콘 캐시 연결 수정** — 백그라운드에서 받은 원본 이미지를 화면 스레드가 즉시 재사용해 글자 타일로 잘못 떨어지지 않도록 개선


### v1.6.47

- **🥈🥇💎 희귀도별 고정 TOP 3** — 챔피언마다 실버·골드·프리즘 증강을 각각 1~3순위 보드로 표시하고, 현재 제시 증강 평가는 별도로 분리
- **🎒 6슬롯 완성 빌드** — 신발을 포함한 아이템 6개를 3×2 카드로 표시하고, 데이터 누락 시 역할 기반 안전 빌드로 중복 없이 보충
- **🤖 아수라장 AI 작전 강화** — 승리 조건·증강 판단·6슬롯 선택 이유·한타 3단계·상대 핵심 대응 순서로 코칭
- **🛡 AI 아이템 검증** — 모델이 슬롯을 빼먹거나 같은 아이템을 반복하면 검증된 Blitz 빌드로 자동 교정


### v1.6.46

- **📅 주간 성장 리포트** — 이번 주와 이전 주의 승률·KDA·평균 데스·CS/분을 자동 비교
- **🎯 숙제 자동 채점** — 반복 데스 패턴으로 배정된 연습 목표를 이후 소환사의 협곡 경기에서 성공률로 정산
- **🔮 습관 스캐너** — 패배 후 20분 내 재큐·새벽 큐·35분 이상 장기전 성적을 표본 3판 이상일 때만 표시
- **🧬 4축 플레이 유형** — 교전·리스크·기복·파밍 점수와 4자리 유형 코드를 8판 이상 표본에서 진단
- **🖼 PNG 성장 카드** — 주간 지표와 숙제 달성률을 공유 가능한 이미지로 저장
- **🔔 상시 자동 복기** — 전적을 불러오면 게임 시작·종료 감지가 함께 켜지고, 양팀 챔피언 브리핑과 종료 복기가 매 게임 재무장
- **🛠 실패 피드백 보강** — AI 생성 준비와 자동 복기 화면 표시 실패를 로그·상태바·알림에 즉시 노출


### v1.6.45

- 🗺 복기 패널에 **킬·데스 지도** — 내 킬(파랑)·데스(빨강 X) 위치를 미니맵에 순서 번호와 함께 표시 (협곡·칼바람 모두)
- 미니맵 클릭 시 확대 지도 + **붕괴 스냅샷** — 30초 내 한 팀 3킬+로 판이 무너진 시점의 10인 위치와 챔피언 아이콘
- 타임라인 요약 수정 — 프레임 내장 이벤트를 읽도록 고쳐 첫 킬·오브젝트 타이밍·골드 격차 흐름이 실제로 표시됨


### v1.6.44 개선

- **🎯 아수라장 챔피언별 추천 동기화** — 173개 챔피언의 Blitz.gg 프리즘·골드·실버 증강 순서와
  코어 아이템을 앱에 반영
- **✅ 전체 parity 검증** — Master Yi를 포함한 전 챔피언 mismatch 0건을 자동 검증하는 validator와
  회귀 테스트 추가


### v1.6.43 개선

- **⏳ 게임 종료 자동 복기 안정화** — 전적 반영이 늦어도 이전 경기를 잘못 복기하지 않고
  「전적 업데이트 중」 상태에서 새 매치가 발행될 때까지 제한적으로 재확인
- **🔎 전적 신뢰도 표시** — 기존 전적 카드에 Riot 출처·패치·표본 수·데이터 시점과
  표본 부족 경고를 표시
- **🎯 반복 패턴 연습 목표** — 최근 5게임에서 충분히 반복된 데스 패턴을 한 가지 행동 목표로
  바꿔 표시 (승률 예측이나 인과관계 주장은 하지 않음)
- **🔕 게임 중 알림 대기** — 게임 중 토스트 알림은 중복 없이 대기하고, 게임 종료 뒤 한 번만 전달


### v1.6.42 개선

- **🌐 메타 데이터 복원력 강화** — blitz.gg 실시간 조회가 막히면 오래된 캐시 사용 시
  결과 상단에 「N시간 전 캐시 사용 중 (현재 패치와 다를 수 있음)」 경고를 표시하고,
  스크래핑 라이브러리 버전 고정을 풀어 차단 규칙 변경에 대응
- **🎯 밴픽 연동 안정화** — 클라이언트 lockfile이 남아 있어도 실제 연결이 안 되면
  조용히 실패하지 않고 명확한 안내를 표시 (LCU 헬스체크)
- **🧵 종료·스레드 안정성** — 앱 종료 중 워커 스레드가 파괴 중인 위젯에 접근하지
  않도록 차단하고, 진행 중인 스레드를 추적·정리
- **🪵 진단 로그 보강** — 아이콘·트렌드·스킨 재렌더 실패처럼 조용히 삼키던 오류를
  로그 파일에 기록해 문제 파악이 쉬워짐


### v1.6.41 개선

- **🔒 자동 업데이트 검증 필수화** — SHA256 사이드카가 없는 릴리스는 다운로드·실행하지 않고,
  설치 파일 크기 상한과 부분 다운로드 정리까지 적용
- **🌐 외부 통신 경계 강화** — 환경 프록시·사용자 CA 우회를 차단하고 Riot·Data Dragon·아이콘
  응답 크기와 JSON 형식, 동일 출처 HTTPS 리디렉션을 검증
- **🗝 비밀값 저장 위치 고정** — 설치본 API 키와 설정을 `%LOCALAPPDATA%\\롤실전코치`에만 저장해
  공유 또는 쓰기 가능한 실행 파일 폴더에 비밀값이 남지 않도록 개선


### v1.6.40 개선

- **🧭 전적 화면 경합 방지** — 리셋·새 로드 뒤 늦게 도착한 전적·아이콘·타임라인이
  현재 화면과 버튼 상태를 덮지 않도록 요청 세대별로 결과 소유권을 확인
- **🛡 입출력·업데이트 안전성 강화** — API 키 숨김 입력, CSV 수식 주입 방지,
  내보내기 경로 오류 안내, 업데이트 버전·응답 크기 검증과 LCU 경고 범위 격리
- **🌐 네트워크 복원력 보강** — Riot 429 비정상 대기값 방어, LLM 응답 스트리밍 상한,
  Riot 매치·타임라인 캐시 경로 통합 및 전체 회귀 테스트 220개 통과


### v1.6.39 개선

- **🛡 Blitz 메타 조회 안정화** — 호스트 검증·응답 크기 제한·리디렉션 제한과
  fresh/stale 디스크 캐시 폴백을 추가해 네트워크 실패 시에도 안전하게 복구
- **📦 Windows 배포 안정화** — frozen 빌드에서 ARAM 카탈로그 리소스를 보장하고,
  다른 모니터에 저장된 창 위치를 현재 화면 안으로 자동 보정
- **🧪 회귀 방지** — Python 3.10·3.11·3.12 CI matrix와 캐시·파서·패키징 테스트 보강


### v1.6.38 개선

- **🎨 티어 표시 정리** — blitz.gg 전환 후 빈 값으로 남던 `티어 ?` 제거
  - 구간(에메랄드+ 등) 표시와 함께 승률·픽·밴 요약으로 정리
- **✏️ 문서·표기 정리** — 사용자 노출 텍스트(API 안내 · --help · 오류 메시지 · AI 프롬프트)와
  내부 주석의 잔여 u.gg 언급을 blitz.gg로 일괄 정리


### v1.6.37 개선

- **🔁 메타 데이터 소스 전면 교체 — 전부 blitz.gg**
  - 협곡 빌드(룬/스킬/스펠/아이템/승·픽·밴률)를 **blitz.gg 단일 소스**로 전환 (u.gg 제거)
  - 카운터도 blitz.gg — **GD@15(Score) 데이터가 그대로 있어** 기존 분석 로직 무변경
  - Cloudflare 차단 없음 — 접속 안정성↑ · 파서 유지보수 1벌로 감소
  - ARAM/아수라장은 기존 Blitz 카탈로그 유지 (u.gg 폴백 제거)
  - CLI `meta`/`coach`도 blitz.gg 전환 (아수라장은 패키지 카탈로그)


### v1.6.36 개선

- **🐛 게임 시작 알림 안정화**
  - 네트워크 오류가 '게임 없음'으로 오인돼 알림이 두 번 울리던 문제 수정
  - 다른 계정으로 전적을 다시 로드해도 **옛 계정 기준으로 감지하던 문제** 수정 (계정 변경 시 watcher 재시작)
- **🐛 ARAM 증강 자동 입력 보강** — 리롤·늦게 도착한 증강도 감지 (챔피언+증강 기준 비교) · 숫자 ID 이름 필터링
- **🐛 스킨 변경 시 큐 필터 유지** — 스킨을 바꿔도 선택한 필터(솔랭/칼바람 등)가 유지됩니다


### v1.6.35 개선

- **🎮 게임 시작 알림** — 앱 실행 중 게임 시작을 감지해 **소리 + 상태바 + 미니 위젯**으로 브리핑
  - 내 챔피언·모드·참가 챔피언 자동 표시 · 설정에서 on/off (1분 간격 폴링, 가볍게 동작)
- **✨ ARAM 제시 증강 자동 입력** — 밴픽 중 LCU에서 **제시 증강을 자동으로 채움** (수동 붙여넣기 불필요)
- **📊 경기 흐름 차트** — 복기 타임라인에 **분당 골드 격차 · 내 CS · 데스** 시각화
- **🔀 내 전적 큐 필터** — 전체 / 솔랭 / 자유랭크 / 일반 / 칼바람 / 아수라장
- **🛡 GUI 실기동 스모크 테스트** — 앱 시작 크래시(v1.6.33) 재발 방지


### v1.6.34 개선

- **🐛 앱 시작 크래시 수정** — v1.6.33에서 타입 프로토콜이 런타임에 tkinter를 가리던 문제
  - `CoachAppAPI`(타입 전용) 스텁이 `after`·`destroy`·초기화를 섀도잉해 실행 직후 `RecursionError`로 종료되던 오류 해결
  - 프로토콜은 타입체크에만 쓰고 런타임 상속을 제거 — 창 생성·이벤트·토스트·종료 정상 동작 확인


### v1.6.33 개선

- **🐛 인앱 자동 업데이트 복구** — 새 버전 감지가 항상 조용히 실패하던 버그 수정
  - v1.6.8 모듈 분리 때 버전 비교 함수의 `self` 파라미터가 빠져 **업데이트 버튼이 활성화되지 않던** 문제 해결
  - 새 버전이 나오면 이제 정상적으로 버튼이 켜지고 상태바에 안내가 표시됩니다
- **⚡ 타입 안정성 완비** — mypy 전 항목 클린 (479 → 0)
  - 탭 믹스인 공용 API를 `CoachAppAPI` 프로토콜로 선언해 타입 검사 재개
  - 파서·아이콘·i18n의 타입 오류, 불필요한 `type: ignore` 9건 정리
- **🧹 내부 정리** — bs4 파서 str 강제 변환 · i18n 변수 섀도잉 제거 · CLI 설정 타입 보강


### v1.6.32 개선

- **🐛 SR 상세 분석 복구** — 상황템·체크리스트 섹션이 안 나오던 버그 수정
  - 적 조합 기반 **상황템 목록**과 **체크리스트**가 정상 표시됩니다
  - 전적 로드 시 자동 복기 설정의 폴백 경로도 함께 복구
- **🛡 화면 오류 안전망** — 창 내부 콜백 오류를 조용히 삼키지 않고 **상태바·로그**로 표시
- **🧹 내부 정리** — 중복 import 제거 · 린트 전 항목 클린 · 테스트 5개 추가 (전체 174개 통과)


### v1.6.31 개선

- **⚡ 스킨 즉시 적용** — 설정에서 스킨 누르면 **재시작 없이** 바로 반영
  - UI를 다시 그려 적용 · 설정 창은 다시 열려 연속 비교 가능
  - 전적 결과·탭 선택 유지

### v1.6.30 개선

- **🧹 스킨 UI 선·보더 정리** — 스킨 바꿔도 레이아웃이 들쭉날쭉하지 않게
  - 전 스킨 **공통 라운드/보더 두께** (카드 12·1, 입력 1)
  - 기본 Frame 보더 0 → 중첩 선·끊긴 선 방지
  - 카드 보더는 액센트 대신 **은은한 구분선** 색

### v1.6.29 개선

- **🎨 UI 스킨 11종** — 다크 7 + **밝은 4**
  - 다크: 클래식 · 네온 · 아쿠아 · 아이스 · 바이올렛 · 오션 · 민트
  - 밝은: **라이트** · **스카이** · **크림** · **블러시**
  - 설정에서 선택 → 앱 재시작 · 헤더 배지로 확인

### v1.6.28 개선

- **🎨 UI 스킨 7종** — 설정에서 선택 (재시작 적용)
  - **클래식** (골드) · **네온** · **아쿠아** · **아이스** · **바이올렛** · **오션** · **민트**
  - 헤더 배지로 현재 스킨 표시 · 언제든 클래식으로 복귀

### v1.6.27 개선

- **🎨 네온 스킨 차이 강화** — 시안 보더·버튼·탭 대비를 키워 클래식과 확실히 구분
  - 헤더에 **「네온 글래스」/「클래식」** 배지 표시
  - 설정에 저장 경로·재시작 안내 강화

### v1.6.26 개선

- **🎨 UI 스킨 2종 (되돌리기 가능)** — 설정 → UI 스킨
  - **클래식 (골드)** — 기존 스타일 그대로 보존 (`theme_classic.json`)
  - **네온 글래스 (실험)** — 레퍼런스 톤 (시안/퍼플 글래스 카드)
  - 선택 후 **앱 재시작** 시 적용 · 언제든 클래식으로 복귀

### v1.6.25 개선

- **🛡 AI 아이템 트리 후처리** — 모델이 템을 짧게 써도 최소 품질 보장
  - 한 줄에 몰린 `1코어…2코어…` **줄 분리**
  - 실명 코어가 **3개 미만**이면 메타 빌드로 빈 슬롯 보충 (AI가 쓴 슬롯은 덮지 않음)
  - 협곡 `coach_comp` · ARAM `coach_aram` 공통 적용

### v1.6.24 개선

- **⚙ 통합 설정 창** — 헤더 「⚙ 설정」
  - **AI 키·모델** · **종료 알림/자동 복기/로드 시 자동 복기** · **화면 배율** · **단축키 안내** · **Riot API 키**
  - 내 전적 탭은 Riot ID·전적 로드에 집중 (설정 UI 이동으로 결과 영역 확보)

### v1.6.23 개선

- **📺 ARAM 입력 접기 · AI 패리티** — 협곡(1.6.15)과 동일한 UX
  - 브리핑 후 입력란 **자동 접기**, 결과·AI 영역 최대화
  - 헤더 「입력 접기/펼치기」 수동 토글 · 초기화 시 입력 다시 펼침
  - AI 상세 코칭은 공용 큰 텍스트박스(17pt, 최대 640px) 그대로 사용

### v1.6.22 개선

- **🛑 게임 종료 시 자동 복기 on/off** — 알림(소리·플래시)과 분리
  - 「게임 종료 시 자동 복기 (패널 열기)」체크박스 (기본 ON)
  - 끄면 상태바만 갱신, 복기 패널은 안 열림 · `ui.json` 저장

### v1.6.21 개선

- **⏯ 전적 로드 시 자동 복기 옵션** — 「내 전적」탭 체크박스
  - **기본 OFF**: 로드 후 안내만 표시, 왼쪽 목록에서 직접 선택
  - ON이면 기존처럼 최근 1판을 바로 염 · `ui.json`에 저장
  - 게임 종료 후 자동 복기는 별도로 유지

### v1.6.20 개선

- **📋 내 전적 경기 목록 우선** — 트렌드·듀오에 가려 경기를 못 찾던 UX 수정
  - **최근 경기 버튼을 맨 위**에 배치 (헤더 바로 아래)
  - 트렌드·듀오는 **「▼ 트렌드·듀오 보기」** 로 접기 (기본 접힘, 펼친 상태 기억)

### v1.6.19 개선

- **↩ 경기 복기 뒤로가기** — 복기 패널 상단에 **「← 목록」·이전/다음** 버튼
  - 목록으로 돌아가 다른 경기 선택 · `1/15` 위치 표시 · 선택 경기 왼쪽 목록 강조

### v1.6.18 개선

- **🔔 게임 종료 알림 on/off** — 「내 전적」탭에서 **소리·작업표시줄 깜빡임**을 끌 수 있음
  - 꺼도 상태바 문구와 자동 복기는 유지 · `ui.json`에 저장되어 재실행 후에도 유지

### v1.6.17 개선

- **🐛 ARAM 메이헴 크래시 수정** — `MayhemCoach`에 없는 `analyze` 호출로
  아수라장 브리핑이 실패하던 문제 (`advise` 호출 + 호환 별칭)

### v1.6.16 개선

- **🛡 AI 아이템 트리 1~5코어** — AI 코칭이 1~2코어만 말하던 문제 수정
  - 메타 코어 + 상황템을 합쳐 **최대 5코어 슬롯**을 프롬프트에 전달
  - AI에 **1~5코어를 각각 한 줄**로 쓰도록 강제 (3코어는 거의 필수, 4·5는 상황/후반)
  - 체크리스트·위젯 요약·규칙 기반 조언도 1~5코어 루트 표시로 통일
  - AI 상세 박스 높이 여유 확대 (최대 640px)

### v1.6.15 개선

- **📺 상세 코칭 최대화** — 분석 후 입력란 **자동 접기**, 결과 영역 거의 전체 사용
  - AI 상세 코칭을 **큰 글씨(17pt) 텍스트 박스**(높이 280~520px)로 표시
  - 헤더 「입력 접기/펼치기」 버튼으로 수동 토글

### v1.6.14 개선

- **👁 결과·AI 코칭 가독성** — 협곡 입력 패널을 콤팩트하게 줄여 결과 영역 확대
  - AI 카드: **상세 코칭을 먼저·큰 글씨**로, 핵심 요약은 아래 작은 글씨
  - 카운터 행·섹션 간격 축소로 스크롤 부담 감소

### v1.6.13 개선

- **🐛 실행 크래시 수정** — `MeTabMixin._profile_labels` 에 빠진 `self` 인자로 앱이 시작 직후 종료되던 문제 수정

### v1.6.12 개선

- **🔍 화면 배율** — 헤더 드롭다운(0.9~1.2x)으로 글자·UI 크기 조절, `ui.json`에 저장
- **🚫 LCU 밴 반영** — 밴픽 연동 시 이미 밴된 챔프는 밴 추천 목록에서 뒤로 +「이미 밴됨」표시
- **⚔ ARAM 조합 카드** — 인게임 자동검색 후 브리핑에 적/아군 태그 기반 위협·시너지 요약

### v1.6.11 개선

- **🖼 트렌드 시각화** — 최근 승패 W/L 칩 + KDA 막대 미니 차트 (내 전적)
- **👥 듀오 통계** — 같이 뛴 소환사 승률·판 수 집계 (2판 이상)
- **🌐 전역 핫키** — Windows에서 `Ctrl+Shift+W` 가 게임 포커스 중에도 위젯 토글 (등록 실패 시 앱 내 단축키만)

### v1.6.10 개선

- **📈 최근 트렌드** — 내 전적 탭에 최근 승률 추세·연승/연패·평균 데스·KDA·CS@10 요약 카드
- **🚫 밴 추천** — 내 챔프를 입력하면 그 챔프를 카운터하는 픽을 밴 후보로 표시 (빠른 추천·상세 분석)
- **⌨️ 미니 위젯 단축키** — `Ctrl+Shift+W` 로 위젯 토글, 위치 기억

### v1.6.9 개선

- **💬 비모달 토스트** — 입력 오류·LCU 실패·복사/내보내기 완료 등을 상태바+하단 토스트로 안내 (인게임 모달 차단 완화)
  - 삭제·업데이트·모드 확인 등 의사결정은 기존 확인 창 유지
- **🧭 에러 문구** — Riot 401/403/404/429·LCU·Cloudflare를 조치 가능한 한국어로 정규화 (`gui/errors.py`)
- **🖼 아이콘 프리페치** — 챔프/아이템 중복 제거 후 필요 크기만 다운로드, 다른 form 로드 시 재렌더 스킵

### v1.6.8 개선

- **🧱 GUI 모듈 분리** — `app.py` 신 클래스(~3.5k줄)를 탭/역할 믹스인으로 분해
  - `sr_tab` · `aram_tab` · `me_tab` · `ai_mixin` · `live_mixin` · `update_mixin` · `constants`
  - 동작은 동일, 이후 탭별 수정·테스트가 쉬워짐

### v1.6.7 개선

- **🛡 안정성·보안** — AI 코칭 generation-id로 늦은 응답 덮어쓰기 방지, 결과 히스토리는 UI 스레드에서만 갱신, 자동 업데이트 **SHA256 검증** (`.sha256` 사이드카)
- **🔑 키·서버 UX** — 기본 플랫폼 `kr`, Development 키 저장 시각·24h 만료 안내, opencode 키 경로 Windows/Linux 다중 탐색
- **📦 캐시·데이터** — 공통 `cache_root()`, blitz.gg 디스크 캐시 72h + 네트워크 실패 시 stale 폴백, 매치 캐시 prune 10분 스로틀
- **🔍 LCU** — lockfile 탐색 경로·레지스트리 보강, 상태바 오타 수정
- **🧰 유지보수** — 로그 핸들러 중복 제거·로테이션, User-Agent 버전 연동, `gui/updater`·`gui/ai_text` 분리, AGENTS 릴리스 예외 규칙

### v1.6.6 개선

- **🧹 화면 인식(베타) 제거** — ARAM 탭의 화면 캡처 증강 인식 기능·`mss`/`numpy` 선택 의존성·관련 테스트/문서를 삭제
  - 증강 입력은 **수동 입력 · 증강 목록 피커 · LCU 밴픽**만 사용
  - 설치본/개발본 모두 미지원 기능 안내가 더 이상 뜨지 않음

### v1.6.5 개선

- **🧭 ARAM 종료 탭 유지** — 게임이 끝나도 현재 보고 있던 ARAM 탭을 `내 전적`으로 강제 전환하지 않음
- **🤖 AI 코칭 가독성 강화** — 골드 테두리·큰 제목·큰 본문으로 AI 카드를 강조하고, 핵심 요약을 카드 맨 위에 별도 표시
  - 미니 위젯에도 AI 전체 응답 대신 우선순위가 높은 핵심 포인트만 요약

### v1.6.4 개선

- **🇰🇷 Blitz ARAM 아수라장 카탈로그** — Blitz의 현재 패치 게임 데이터에서 231개 증강의 공식 한글명·설명·희귀도·티어를 다시 수집
  - 모든 증강 설명을 u.gg가 아닌 Blitz 한국어 데이터 기준으로 통일
  - 모든 증강 아이콘을 Blitz CDN에서 검증해 카드와 아이콘 캐시에 반영
  - 전 챔피언 173명의 Blitz ARAM-Mayhem 완성 아이템 코어 순서와 챔피언별 빌드 링크를 패키지
  - ARAM 결과의 출처와 패치 표시도 Blitz 카탈로그 기준으로 고정하고, u.gg는 Blitz 데이터 누락 시에만 폴백

### v1.6.3 개선

- **📅 현재 패치 기준 AI 코칭** — 오늘 날짜와 분석 패치를 프롬프트에 고정해 이전 시즌 수치·메타를 현재 내용처럼 단정하지 않도록 강화
  - 협곡 상세 정보에 적 정글·서폿·탑·미드·원딜과 내 챔피언이 모두 입력되면 상대 5인 조합에 맞춘 한타·진입·보호·오브젝트 운영을 우선 코칭
  - AI 코칭 카드가 결과 맨 위에 표시되어 긴 결과를 스크롤하지 않아도 바로 확인
- **🔮 ARAM 아수라장 실전 코칭** — 실행 중 게임 자동 검색으로 저장한 우리 조합·상대 조합을 AI에 함께 전달
  - 조합에 맞는 플레이 방식, 제시 증강 우선순위, ARAM 아이템 빌드를 한 번에 안내
  - 규칙 기반 u.gg/Data Dragon 결과와 함께 현재 패치 기준으로 표시
- **🔑 AI 설정 분리** — 내 전적 탭에서 API 키(마스킹 입력/저장)와 AI 모델을 별도 행으로 설정
  - `deepseek-v4-flash`, `deepseek-v4-pro`, `kimi-k3`, `glm-5`, `qwen3.7-plus` 선택 지원
- **🛡 게이트웨이 장애 폴백** — 500 응답·타임아웃을 3회 재시도하고, 실패하면 규칙 기반 결과를 유지해 화면이 멈추지 않음

### v1.6.2 개선

- **🧠 AI 모델 선택** — 내 전적 탭 드롭다운으로 모델 변경 (기본 `deepseek-v4-flash` · `deepseek-v4-pro` · `kimi-k3` · `glm-5` · `qwen3.7-plus` — 모두 opencode-go 게이트웨이 동작 확인)
- **📌 미니 위젯 AI 코칭** — AI 코칭 결과가 미니 위젯에 바로 표시 — 결과 화면을 스크롤하지 않아도 게임 위에서 확인

### v1.6.1 새 기능

- **🤖 선택형 AI 코칭** — 규칙 기반 결과 위에 진짜 자연어 코칭을 얹습니다
  - opencode-go 게이트웨이(`deepseek-v4-flash`) 연동 — 빠른 추천/상세 분석/경기 복기 결과마다 **AI 코칭 카드** 추가
  - **키 자동 감지**: opencode CLI 인증 파일을 같은 PC에서 자동으로 찾음 → 별도 설정 없이 바로 활성
  - **수동 입력도 가능**: 내 전적 탭의 「AI 코칭 키」 칸에 붙여넣기 (비우면 자동 감지)
  - 키가 없거나 생성 실패해도 **기존 규칙 기반 결과로 폴백** — 기능은 그대로 동작
  - 비용 부담 없음 (요청당 수백~수천 토큰, 개인용량 수준) · 키는 저장소에 절대 포함되지 않음

### v1.6 새 기능

- **🎨 GUI 전면 리디자인 (다크 리그 에디션)** — 기능은 그대로, 디자인만 새로
  - LoL 클라이언트 스타일 **딥 네이비 + 골드 팔레트** 커스텀 테마 (기본 블루 → 골드 `#C8AA6E` 액센트)
  - 섹션 헤더 **골드 액센트 바**, 카드형 결과 행(보더+라운드), **S/A/B/C 등급 칩** (카운터 GD@15·증강 티어)
  - 탭 선택 골드 강조 + 버튼 변형 체계(골드/패널/초록/보라), 입력칸·드롭다운·스크롤바 전면 정돈
  - 미니 위젯 — 골드 상단 라인 + 카드 바디
- **🧪 기능 회귀 없음** — GUI 테스트 포함 전체 pytest 통과 (디자인 토큰 문서: `docs/concepts/`)

### v1.5 새 기능

- **⬆ 자동 업데이트 알림** — 앱 시작 시 GitHub 최신 릴리스 확인, 새 버전이 있으면 상태바에 안내
- **📈 타임라인 복기** — 경기 복기에 15분 내 골드 · 첫 킬/포탑 · 용/바론/전령/공허유충 처치 타이밍 추가 (Match V5 Timeline)
- **📌 미니 위젯 개선** — 위젯 클릭 시 메인 창 복귀 + 📋 복사 버튼 (인게임에서 바로 활용)
- **🖼 아이콘 버전 영속화** — Data Dragon 버전을 캐시에 저장, 오프라인에도 마지막 성공 버전 사용 (신규 챔피언 아이콘 보존)
- **🧪 GUI 테스트 안정화** — Tk 이미지 네임스페이스 충돌 제거로 flaky 테스트 수정
- **📦 빌드 안정화** — PyInstaller hiddenimports 보강 (함수 내 import 모듈 누락 방지)

### v1.5.1 개선

- **📈 타임라인 디스크 캐시** — 복기 타임라인도 `cache/timelines/`에 저장, 재조회 시 API 재호출 없음 (정리 규칙 포함)
- **🛡 단일 인스턴스** — exe 중복 실행 시 "이미 실행 중" 경고 후 종료 (LCU/워처 중복 폴링 방지)
- **🔢 경기 수 선택** — 전적 탭에서 최근 5~50경기 드롭다운으로 선택 (기존 15경기 고정)
- **🧹 빌드 정리** — 빈 루트 `data/` 참조 제거 (패키징은 `src/lol_coach/data`만)

### v1.5.2 개선

- **🗑 프로필 삭제 버튼** — 전적 탭에서 저장된 프로필을 목록에서 바로 제거 (Riot 계정·전적엔 영향 없음)
- **📄 로그 파일 저장** — `logs/lol_coach.log`에 기록 (설치본 문제 진단 시 이 파일 공유)
- **🪟 창 크기/위치 기억** — 닫을 때 `ui.json`에 저장, 다음 실행 시 복원
- **🔁 릴리스 안정화** — 테스트가 Windows Tk 일시 오류로 실패해도 1회 재시도 (릴리스 중단 방지)

### v1.5.3 개선

- **🧹 탭별 초기화 버튼** — 협곡/ARAM/전적 탭에서 입력·결과를 한 번에 비움 (LCU 밴픽 추적도 함께 중단)

### v1.5.4 개선

- **🔄 앱 내 자동 업데이트** — 업데이트 버튼 한 번으로 최신 인스톨러 다운로드 → 무음 설치 → 새 버전 자동 재실행 (설정·캐시·프로필 유지, 캐시 재사용)

### v1.5.5 개선

- **🔑 API 키 보호** — 전적 탭 초기화가 API 키 입력칸을 비우지 않음 (.env 키 유지)
- **🔧 전적 로드 버튼 잠김 수정** — 초기화 후에도 로드 버튼 정상 동작 (자동 로드와의 busy 경합 해소), 로드 중 클릭 시 상태바 안내

### v1.5.6 개선

- **⚡ 전적 로드 즉시 표시** — 아이콘 수백 개 다운로드가 끝날 때까지 기다리지 않고, 데이터를 먼저 렌더링 (아이콘은 백그라운드 프리페치 후 채워짐)
### v1.4 새 기능

- **🎯 LCU 밴픽 연동** — 게임 클라이언트 로컬 API로 **챔피언 셀렉트 중** 적/내 픽을 읽어 바로 카운터 추천 (API 키 불필요, Spectator로 안 되던 밴픽 단계 커버)
- **🔔 게임 종료 자동 복기** — 인게임 자동입력 후 종료를 폴당해 방금 판 복기를 자동 표시
- **🏅 랭크 표시** — League V4 솔로/자유 랭크 티어·LP·승률 (GUI 내 전적 탭 + CLI `profile`)
- **📊 챔피언 풀 진단** — 베이지안 보정 승률 기반 집중/유지/정리 추천 (GUI + CLI `pool`)
- **📌 미니 위젯** — 마지막 분석 요약을 항상 위 창으로 (인게임 중 Alt+Tab 불필요)
- **📁 전적 내보내기** — CSV/JSON 파일로 저장 (GUI 버튼 + CLI `export`)
- **👤 멀티 프로필** — 여러 Riot ID 저장·전환 (`profiles.json`)
- **💡 아이템 툴팁** — 상세 분석 아이템에 마우스를 올리면 가격·설명 표시
- **⚡ 성능** — 매치 상세 병렬 조회 + 디스크 캐시(`cache/matches/`)로 재조회 즉시 완료
- **🔧 로깅** — CLI `-v/--verbose` 또는 `LOL_COACH_DEBUG=1`로 네트워크 진단 로그
- **⌨️ 챔피언 자동완성** — 협곡 탭 7개 입력(적 라이너·내 챔프·정글·서폿·탑·미드·원딜) 한글/영문 자동완성 (Windows IME 조합 문자 지원)
- **🔄 LCU 밴픽 자동 추적** — 밴픽 중 픽이 바뀔 때마다 4초 간격 자동 갱신, ARAM 밴픽은 협곡 탭에서 눌러도 탭 자동 전환
- **🗂 탭별 동시 작업** — 협곡 분석 중에도 전적 로드·ARAM 브리핑 실행 가능 (글로벌 잠금 → 작업별 잠금)
- **📜 결과 히스토리 + 📋 복사** — 최근 20개 결과 복원 버튼, 요약 클립보드 복사 버튼
- **🗂 증강 목록 피커** — 카탈로그 200+종 검색 팝업에서 클릭으로 제시 증강 입력
- **⚡ 시작 시 자동 로드** — 마지막 프로필 전적 자동 로드 + 서버 선택 드롭다운
- **💾 blitz.gg 디스크 캐시** — 빌드 데이터 영속 저장, 재시작·일시 차단에도 마지막 빌드 복원
- **🔔 종료 알림** — 게임 종료 시 비프음 + 작업표시줄 플래시, 폴링 간격 45초→15초 단축
- **🧹 캐시 자동 정리** — 매치 캐시 30일 경과분 + 1000개 초과분 자동 삭제
- **⏰ API 키 만료 안내** — 401/403 응답 시 "키 재발급" 안내 (개발 키 24시간 만료)

### Modes

| Mode | Riot queues | blitz.gg source |
|------|-------------|-----------------|
| `summoners_rift` | 400/420/430/440… | `/lol/champions/{champ}/build?role={ROLE}` |
| `aram` | 450 ARAM + **2400 ARAM Mayhem** | 패키지된 Blitz 카탈로그 (`blitz_aram_builds.json`) |

## Project layout

```
lol-coach/
├── main.py                 # CLI entry
├── gui_main.py             # GUI entry
├── requirements.txt
├── .env.example
├── src/lol_coach/
│   ├── cli.py              # click commands (setup/profile/live/meta/coach/pool/export)
│   ├── config.py           # API key / .env / 멀티 프로필(profiles.json)
│   ├── lcu.py              # LCU lockfile + 챔피언 셀렉트 파싱
│   ├── log.py              # 공통 로깅 (-v/--verbose)
│   ├── riot/               # Account, Match V5(+병렬/캐시), League V4, Spectator
│   ├── blitz/              # blitz.gg fetch + HTML parse (빌드/카운터)
│   ├── static/             # Data Dragon / 로컬라이저 / 아이콘·증강 카탈로그
│   ├── analysis/           # coach · 복기 · 조합 · 아수라장 · 풀 진단 · 내보내기
│   └── gui/                # app · 자동완성 · 미니 위젯 · 툴팁 · 종료 감지 워처
└── tests/
```

## GUI (롤 실전 코치)

```powershell
cd C:\Users\hyj53\lol-coach
$env:PYTHONPATH = "src"
pip install customtkinter
python gui_main.py
```

| 탭 | 기능 |
|----|------|
| **소환사의 협곡** | 적 라이너(+정글/서폿/전체) → 카운터 · 조합 위협 · 용/바론 · 코어+상황템 · 체크리스트 · **LCU 밴픽 불러오기** · 아이템 툴팁 |
| **ARAM 아수라장** | 챔피언 + **게임에서 제시된 증강** → Top5 추천 · 피할 증강 · 칼바람 빌드 · 실전 팁 · **LCU 내 픽 입력** · **증강 목록 피커** |
| **내 전적** | 내 Riot ID 연동 · 최근 경기 · 챔프별 성적 · **랭크** · **챔프 풀 진단** · **CSV/JSON 내보내기** · **멀티 프로필** · **게임 종료 자동 복기** |

공통: 헤더의 **📌 미니 위젯**으로 마지막 분석 요약을 항상 위 창에 띄울 수 있습니다.

## LCU 밴픽 연동 (신규)

GUI 각 탭의 **🎯 밴픽 (LCU)** 버튼은 게임 클라이언트의 로컬 API(lockfile)를 읽어
**챔피언 셀렉트 중인 픽/밴**을 가져옵니다.

- Riot API 키 없이 동작 (로컬호스트 통신만 사용)
- 협곡 탭: 적 5명 픽 → 포지션 추정(태그 기반, 부정확할 수 있음) + 내 픽/포지션 자동 입력 → 즉시 카운터 추천
- ARAM 탭: 내가 고른 챔피언 자동 입력
- 클라이언트가 꺼져 있거나 밴픽 중이 아니면 안내 메시지만 표시
- lockfile 경로가 기본값(`C:\Riot Games\League of Legends\lockfile`)과 다른 경우
  환경변수 `LOL_LOCKFILE`로 지정

## Setup

```powershell
cd lol-coach
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pip install -e .
```

`pyproject.toml` 없이 바로 쓰려면:

```powershell
$env:PYTHONPATH = "src"
python main.py setup
```

### Riot API Key

1. https://developer.riotgames.com/ 에서 Development API Key 발급
2. 아래 중 하나:

```powershell
python main.py setup
# 또는
python main.py setup --api-key "RGAPI-...." --riot-id "소환사명#KR1" --platform kr
```

키는 프로젝트 루트 `.env`에 저장됩니다. **개인 키는 24시간마다 만료**됩니다.

## Commands

| Command | Description |
|---------|-------------|
| `setup` | API 키 / 기본 소환사 설정 |
| `test-key` | 키 유효성 검사 |
| `profile` | 최근 전적 분석 (+ 랭크 표시) |
| `live` | 현재 인게임 여부 |
| `meta <champ> -r mid` | blitz.gg 메타 빌드만 |
| `coach <champ> -r mid` | 메타 + 내 기록 맞춤 코칭 |
| `pool` | 챔피언 풀 진단 (집중/유지/정리) |
| `export` | 최근 전적 CSV/JSON 내보내기 |
| `gui` | 데스크탑 GUI 실행 |

모든 명령은 `-v/--verbose`로 디버그 로그를 켤 수 있습니다 (`lol-coach -v profile`).

### Examples

```powershell
$env:PYTHONPATH = "src"

python main.py setup --api-key "RGAPI-xxx" --riot-id "소환사명#KR1" --platform kr
python main.py test-key
python main.py profile --count 15
python main.py live
python main.py meta Ahri -r mid
python main.py meta Ahri -m aram
python main.py coach Ahri -r mid --lookback 20
python main.py coach Ahri -m aram --lookback 20

# ARAM Mayhem: GUI의 "제시 증강" 칸에 게임에서 보이는 증강을 쉼표 또는 줄바꿈으로 직접 입력
# CLI의 `-m aram`은 패키징된 아수라장(ARAM Mayhem) 메타 카탈로그를 조회합니다
# (네트워크 없이 동작; 제시 증강 기반 브리핑은 GUI에서 제공).
```


## How blitz.gg data is fetched

1. `cloudscraper`로 챔피언 빌드 페이지 HTML을 가져옵니다 (허용된 blitz.gg 호스트만 접근).
2. BeautifulSoup으로 **현재 패치, 승/픽/밴률, 룬(rune-img.active), 스킬 우선순위, 소환사 주문(CDN id), 코어/시추에이셔널 섹션**을 파싱합니다.
3. 결과는 메모리 5분 + 디스크 72h 캐시됩니다 (네트워크 실패 시 stale 폴백).

> 아이템은 완성/시추에이셔널 그룹을 사용하고, 신발은 별도 boots 섹션으로 분리합니다.

## ARAM Mayhem 코칭

### 입력

- 사용자가 **게임 안에서 실제로 제시된 증강 이름**을 쉼표/줄바꿈으로 입력합니다.
- 제시되지 않은 증강은 추천·회피에 포함되지 않습니다.
- 한글/영문/별칭을 모두 인식하며, 중복과 카탈로그에 없는 이름은 별도로 안내합니다.

### 개인화 로직

- 챔피언 성향(`Mage`, `Marksman`, `Assassin`, `Fighter`, `Tank`, `Support`)을 Data Dragon 태그로 판단합니다.
- 제시된 증강 중 **S/A 등급 + 챔프 성향 시너지**를 우선 추천하고, 성향 충돌이나 B등급은 회피로 분류합니다.
- 아이템 빌드는 패키지된 **Blitz 카탈로그**를 사용하고 누락 시 클래식 ARAM 폴백을 제공합니다.

### 출처

- 증강 데이터: 패키징된 `src/lol_coach/data/aram_mayhem_augments.json` 기준.
- 아이템/스킬 빌드: 패키지된 Blitz 카탈로그 (`blitz_aram_builds.json`).
- 팁: Data Dragon 스킬 정보 + 제시된 증강 위주의 조합 해석.

### 이미지

- 증강 아이콘은 **카탈로그에 등록된 정확한 후보 URL**만 사용합니다.
- 우선순위: Riot Data Dragon / Riot 패치 노트 / blitz.gg / League Wiki / ARAM Mayhem 커뮤니티(arammayhem.com).
- 네트워크 실패·검증 실패 시 이미지 대신 **이름+등급 카드**를 표시합니다.

## Cache

- 챔피언·아이템·증강 아이콘은 Data Dragon/blitz.gg/커뮤니티에서 받아 **로컬에 캐시**합니다.
- **매치 데이터도 디스크 캐시**(`cache\matches\{match_id}.json`) — 매치 payload는 불변이라
  한 번 받은 경기는 API 재호출 없이 즉시 로드됩니다. (`RiotClient(use_cache=False)`로 해제 가능)
- 캐시 위치: 설치본은 `%LOCALAPPDATA%\롤실전코치\cache\`, 포터블/개발 환경은 exe/프로젝트 옆 `cache\`.
- GUI는 브리핑 생성 시 백그라운드 스레드에서 필요한 아이콘을 프리패치합니다.
- 이전에 성공한 이미지 URL은 `last_known_good` 인덱스로 유지되어, 오프라인/일시 장애 시에도 마지막으로 확인된 아이콘을 보여줍니다.
- 이미지 파일이 깨지면 다음 요청 때 자동으로 다시 받습니다.

## Maintainer check

릴리스 전 카탈로그 상태를 점검하려면:

```powershell
python scripts/refresh_aram_mayhem_data.py
```

blitz.gg HTML 구조가 바뀌어 파싱이 깨졌는지 확인하려면:

- `tests/test_parser.py`의 인라인 샘플(`tests/blitz_samples.py`)이 실페이지 구조와 어긋나면
  파싱 회귀가 발생한 것이므로, 실페이지에서 샘플을 다시 캡처해 갱신합니다.
- GUI에서 빌드/카운터 로드가 실패하면 상태바에 오류가 표시되고, 캐시가 있으면 stale 폴백이 동작합니다.

- `data/aram_mayhem_augments.json`의 스키마·중복·ID·등급 유효성을 검사합니다.
- 모든 증강에 **≥128 px 후보 이미지**가 있는지 확인합니다.
- `--patch 15.x`로 기대 패치를 지정할 수 있습니다.
- `--require-full-coverage` 기본값은 `true`: 이미지 후보가 없는 증강이 있으면 비제로 종료합니다.
- `--allow-community-only` 기본값은 `true`: Riot/Wiki 자동 검증이 어려울 때 arammayhem.com 커뮤니티 자산을 정확한 후보로 허용합니다.

이 스크립트는 **이미지 URL을 새로 만들지 않습니다**. 기존 카탈로그의 내보내기 출처와 해상도 제약만 검증합니다.

## Notes

- 기본 플랫폼: `na1` / routing `americas`
- Rate limit(429) 시 `Retry-After`를 존중합니다.
- blitz.gg 이용약관에 유의하세요. 개인/학습 용도로만 사용하세요.

## License

MIT (personal project)
